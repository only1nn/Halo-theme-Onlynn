# 更新日志

Onlynn 的基础样式取自 [Fuwari](https://github.com/saicaca/fuwari)，移植到 Halo 之后按
[Firefly](https://github.com/CuteLeaf/Firefly) 的美学补齐了外观与交互定制能力。
本文件只记录 **Onlynn 自己的变更**；上游项目的历史请见各自仓库的 CHANGELOG。

---

## v1.2.0

### 新增

- **新增「增强功能」分组**（第 11 个顶层分组），收纳下面四组与外观强相关、但和布局 / 样式
  两条主线不同的设置：离屏文案、鼠标风格、鼠标特效、灰色模式

- **鼠标风格** —— **14 套内置光标**，选项：（OwO / UwU / 清风 /
  卡通圆润 / 彩虹水滴 ×2 / 彩虹小马 / 彩色碎片 / 守望先锋 / 彩虹云雨 / 小樱茉莉 /
  黑色小猫 / 音乐小猫 ×2），每套都是指针 / 手型 / 文本 / 缩放四态；少数几套的四态用不同
  文件，与该主题逐条对齐。另有「自定义上传」：四态各一个槽位，可只填部分，没填的回落到
  系统光标，`.png` / `.svg` 可配热点偏移（`.cur` / `.ani` 自带热点）。触摸设备上不生效
  - 四态变量的值由构建期依据一张映射表生成，**服务端渲染，不依赖脚本** —— JS 没跑或挂了
    光标照样是选中的那套，两条路都带关键字兜底（图加载失败退回系统光标）

- **鼠标特效** —— 选项：移动 8 种（气泡跟随 / 表情包跟随 /
  弹性表情包跟随 / 仙女棒效果 / 雪花跟随 / 圆点跟随 / 移动残影（疏）/ 移动残影（密））+
  点击 4 种（烟花特效 / 粒子爆炸 / 富强民主 / 爱心特效），另有特效不透明度与移动端开关。
  实现上补齐了这套特效普遍缺的性能护栏：**粒子有硬上限 + 对象池**（移动 ≤150、单次点击 ≤60、
  总数 ≤300）、**没有粒子就停掉逐帧循环**（不常驻，跟随类特效停下也不再排帧）、
  **标签页隐藏时停帧**、**指针事件按距离与时间双阈值节流并合并到一帧**、**DPR 感知且 resize
  重建画布**、**系统「减少动效」时完全不启动**、**触摸设备默认不启用**、**单次绑定守卫**。
  两个特效都选「关闭」时脚本不下载

- **离屏文案** —— 切到其它标签页 / 后台时把标题换成「离开」文案，回来时短暂显示「回来」
  文案再自动恢复，停留秒数可配。与原参考实现的差别：换页时若页面可见就**提前更新原标题**
  （否则换页后要等一次隐藏才更新）、加绑定守卫（重复执行不会把「回来」文案记成原标题而
  锁死）、隐藏 / 离页时清定时器

- **灰色模式** —— 哀悼日等场景按日期段全站去色，支持多段、支持跨年段（如 `12/30~01/02`，
  单 / 双位数字与 `~ - ～` 三种分隔都认）。**判定放在首帧**（内容解析前给 `<html>` 加类），
  不会先出彩色再变灰；留空表示长期生效，**格式写错则按不生效处理**（否则一个笔误就会让
  全站长期变灰）

- **文章热度 ℃**（文章 → 文章信息）—— 按 `24 + 浏览量×0.1 + 点赞×2 + 评论×3` 在服务端算出
  热度值，分三档配色（越热越暖）。列表卡片与文章页都显示，数据取自 Halo 自带的
  `post.stats`（浏览 / 点赞 / 评论）

- **插件的 CSS 变量对齐**（`src/styles/plugin-vars.css`，纯 CSS 无脚本）—— 把主题的配色与
  圆角映射给**搜索组件**与**编辑器超链接卡片**的官方 CSS 变量，插件页面不再跳出色系。
  变量名逐个对着插件的发布源码核对；找不到公开源码的插件不收录，宁可不写也不放一批
  可能不存在的变量名

- **访客开关 +2** —— 「显示设置 → 特效」新增**自定义光标**与**鼠标特效**两个开关，访客能
  自己关掉（指针特效最容易被嫌吵）。随「访客可调样式」总开关一起收回

- **构建期体检 `scripts/check-template-integrity.mjs`** —— 串进 `pnpm verify`（`pnpm build`
  会先过这道关），查两件事：① 产物模板的 `<script>` 内容与 HTML 注释里出现裸的方括号加
  圆括号组合即报错（`th:inline="javascript"` 的那几处豁免）；② `script`/`style` 开闭标签
  数量是否配对。这两类问题 `astro build` 都看不见，且**是否炸取决于后面跟了什么内容**，
  属于随机爆的隐性故障，必须在构建期拦掉

### 修复

- **留言板整页打不开（影响最大）** —— 打开留言板会一直转圈、服务端抛
  `TemplateProcessingException: Could not parse as expression` 后中断响应，浏览器看到的是
  页面永远加载不完。根因是 **Thymeleaf 在 HTML 模式下默认把文本里的内联标记 `[(` / `[[`
  当表达式处理**，而音乐播放器的 LRC 时间戳正则里正好出现「方括号紧挨左圆括号」这三个
  字符，Thymeleaf 一路找配对的括号，把后面大段脚本拿去当表达式解析就崩了。改用方括号的
  十六进制转义写法，语义完全一致 —— **加 `th:inline="none"` 挡不住**（实测：那个脚本本来
  就带着它，照样炸）
- **留言板「样式没了、正文不见了」** —— 上一条修完后页面能打开了，但只看得见开头一小段，
  后面的卡片、侧栏、页脚全不见。根因在构建期的注释剥离脚本：它先按 `<script>`/`<style>`
  把 HTML 切开、再在剩下的片段里删注释，于是**注释里出现的 `<style` 字样**被当成了真正的
  样式块起点 —— 它「保护」了注释的后半截一直到下一个 `</style>`，同时删掉了注释开头，
  留下一个裸露的 `<style>` 开标签，浏览器把后面三十万字符的正文全当 CSS 文本吞掉。
  改成从左到右单遍扫描（注释丢、script/style 原样留），并补了回归用例
- **错误页（404 / 500）自身报错** —— 访问不存在的路径时连错误页都渲染不出来。根因与第一条
  同类：我自己写的 HTML 注释里带了那种内联标记当示例文字，而 Thymeleaf 连注释一起处理，
  于是拿三个点去求值，报 `Exception evaluating SpringEL expression`
- 统计组件里两处 markdown 清理正则同样含那三个字符，一并换成转义写法（属于同类隐患，
  只是暂时没炸）

- **灰色模式不能用 `filter` 挂在 `html` 上** —— `filter` 会让该元素成为所有 fixed 后代的
  包含块，挂上去会让导航栏、浮动按钮、移动端底栏改成相对 `html` 盒子定位、跟着页面滚走。
  改用一层 `backdrop-filter: grayscale(1)` 的覆盖层：灰度是逐像素运算，先合成再整体去色
  与逐层去色结果一致，但完全不影响其它元素的定位（已在浏览器里实测 5 个 fixed 元素
  滚动 500px 后位置不变）
- **鼠标特效的「圆点跟随」停不下来** —— 淡入与淡出写在同一帧的两条互不冲突的分支里，
  净效果是每帧 +0.01 的不透明度，圆点的逐帧循环永远不会退出。改为互斥分支
- **跟随类特效（圆点 / 两种残影 / 弹性表情）的「空闲」判定按帧计** —— 原先按墙钟（距上次
  移动 1.2 秒）判断，而后台标签页会把定时器节流到 1 秒一帧，于是出现「刚画完一帧就被判
  空闲、紧接着被收尾的清屏擦掉」，整条残影在浏览器里根本看不见（Node 桩替身测不出来，
  因为它那里的时钟是可控的）。改为按帧计数，且跟随类特效停下时保留最后一帧：鼠标停住后
  残影留在原地，再动就接上去
- **粒子迸发的两个调色板项明度写成 100%（等于纯白）** —— 叠色后糊成一团白斑；同时迸发粒子
  原先不随寿命收小与淡出（60 帧后整块消失），现改为边飞边收小、末段淡出

- **一言刷新不更新，取不到时还会被兜底句钉住** —— 原实现把 `sessionStorage` 里的缓存当唯一
  数据源：整页加载（含 F5）只要读到缓存就直接显示，而且**取不到一言时会把兜底句写进缓存**，
  于是一次网络抖动就把这一句钉死在整个标签页会话里，刷新也不再动。现在整页加载一律重新取
  （缓存只用来先把上一句显示出来，避免空着），取不到不写缓存，请求加 `no-store` 免得中途
  缓存把同一句复用回来；「换一句」按钮仍是一次点击换一句
- **左右侧栏都放一言之日，只有左边那张会更新** —— 节点是用 `getElementById` 找的，第二张
  卡片拿不到，永远停在「加载中」。现在按 `data-hitokoto-*` 遍历卡片各管各的，同一份文档里
  的多张卡片共用一次请求（不会因为放了两处就请求两次）

### 优化

- **侧栏偶尔「过一秒才显示出来」** —— 入场动画是「`opacity: 0` + 动画补间到可见」的写法，
  一旦动画没能正常跑完，元素就停在透明状态。两种情形实测都会发生：动画被浏览器挂起
  （标签页在后台、主线程被占时 `startTime` 一直是 null），以及动画被重建后重放
  （JS 包裹滚动容器/重排布局会让浏览器重建动画对象，首轮还没结束就又重建）。
  而摘掉 `onload-animation` 类只靠 `animationend`，动画没跑完就永远等不到那个事件。
  现在加了兜底清扫：入场窗口后（600 / 1500 / 3000ms）扫一遍，元素上的动画**没在跑**就摘类、
  回到静态可见状态。判据是「动画没在跑」而不是「过了多久」，所以自定义档把时长拉长也不会误伤。
  实测：人造一个延迟 60 秒的入场元素，约 1.1 秒内被救回可见；正常元素不受影响
  兜底之外还做了釜底抽薪的一步：**两侧边栏不再做入场淡入**，直接以可见态参与首帧。
  边栏内容是静态的、位置在页面两端，这条淡入观感收益很小，却是「侧栏晚出现」的唯一来源
  （主题本来就已经对换页后的右栏强制无动画，这次统一到首次加载）。
  实测：侧栏从第一帧起 `opacity: 1` 且不再持有任何动画对象。正文与卡片的入场动画保留。

- **图片按槽位取尺寸（线上首页少下约一半字节）** —— Halo 2.22 起会给没有 `srcset` 的
  `<img>` 自动补响应式 srcset，并在**主题没写 `sizes` 时**按「整栏宽度」猜一个默认值
  （`(max-width:640px) 94vw, …, min(800px,85vw)`）。于是小槽位也按 800px 选图：站长的
  首页 5 张卡片封面实际只有 154px 宽却各取了 800px 的图（合计约 2.1MB），导航栏 28×28
  的 logo 取了 800px（256KB）。现在主题给卡片封面、站点 logo、菜单图标、页脚友链 logo、
  音乐播放器封面等都写了准确的 `sizes`（有 srcset 的图 Halo 会跳过，所以自己写准就生效）。
  实测同一张封面图从 `?width=800` 改为 `?width=400`，单张体积降到约三分之一
- **副标题首屏即渲染** —— 原先 `#banner-subtitle` 初始只有一个空格，要等 banner 脚本
  （defer 加载、开局还再停 500ms）才开始逐字打，慢网络下首屏会空白一两秒，观感上像是
  「标题先出来、副标题后出现」。现在解析到副标题时就由一段内联脚本把第一行填进去，
  打字机识别到这段已渲染的文字后从「第一行已打完」接着往下走（不清空重打），
  停留 2 秒后照常进入回退/重打的循环 —— 首屏立刻可见，动效一点没少

- **鼠标特效改成「按需启动」：不动鼠标就不干活** —— 原先页面一加载就建一整屏画布
  （DPR 2 的 4K 屏上是 3840×2160 ≈ 33MB）、烘一批离屏精灵（每张都要 measureText /
  fillText，会碰字体匹配）、再排一个逐帧回调，而多数访问根本不会动鼠标。现在这些全部
  推迟到**第一次指针事件**才做：实测首页 DOMContentLoaded 从 537ms 降到 490ms，
  加载完成时页面上只有樱花那张画布。鼠标一动照样立刻出特效（首帧就有粒子）
- **精灵按需构建** —— 只做当前特效要用的那几张：只开「点击特效」时一张都不烘
  （原先会把 3 个星尘 + 5 个表情 + 雪花 + 箭头全烘一遍）；表情特效也只烘配置里列出的
  那几个表情

### 变更

- 后台设置分组 10 → 11（新增「增强功能」）
- 「文章 → 文章信息」多一项「显示文章热度」（默认关）
- 一言的「获取失败文案」默认句子改为「愿做檐下一株草，不争不抢，自在生长。」
  （左右侧栏两处设置默认值与脚本兜底同步；已在后台保存过该字段的站点不受影响，
  想用新句子需在「侧边栏 → 小组件设置 → 一言」里手工替换）
- 主题首次打包第三方素材：14 套光标共 37 个 `.cur` 文件（约 205KB），出处见
  `public/assets/cursors/SOURCES.md`；

---

---

## v1.1.0

> 从这一版起进入 1.1 号段：1.0.x 固定为已发布的稳定版，新功能进 1.1。

### 新增

- **页脚链接栏** —— 页脚的多栏链接区，栏目标题与链接都可在后台配。数据直接用
  **Halo 菜单**：一级菜单项作为栏目标题，二级项作为该栏的链接，因此排序、嵌套、
  图标都能用 Halo 原生的菜单编辑器维护，不用在主题设置里再造一套。
  设置项：「页脚 → 页脚链接栏」（启用 / 区块标题 / 选择菜单 / 仅在首页显示 / 每行列数）。
  整组按内容宽度居中，只配一两栏也不会贴着左边
- **副标题「打字机回退效果」开关**（样式设置 → 标题与副标题）—— 开启后打字完成会逐字
  回退再重新打字，形成循环；关闭则打完即停。默认开启。副标题写了多行时本来就逐条循环，
  此开关只决定「最后一轮打完后要不要接着打」
- **Hero 内页背景虚化**（样式设置 → 显示设置面板 → 壁纸默认值）—— 全屏 + Hero 下，
  首页保持清晰（滚动时逐步虚化），其余页面的壁纸按此值常驻虚化，让浮在壁纸上的内容
  更清楚；默认 8px，填 0 可关闭
- **友链页「一键复制全部信息」** —— 左侧自身信息卡底部一个按钮，一次复制五行：
  站点名称 / 站点描述 / 站点链接 / 头像链接 / RSS 地址。格式与字段文案在
  `i18n/*.properties` 的 `page.links.copyAllTextA/B` 里，可翻译

### 修复

- **访客可调样式总开关失效（影响最大）** —— 访客开关的配置路径在改版时迁到了
  `style.displayPanel.visitorStyle`，但前台仍读旧路径 `style.visitorStyle`（站点配置里
  两处同时存在、旧值恒为开启），导致后台关掉总开关后，前台按钮与面板照样出现、
  访客仍能改样式。已统一到新路径；顺带补齐入口与面板的开关白名单
  （原先漏了樱花 / 弹幕 / 侧栏位置三项，会出现「只开樱花却看不到入口」）
- **Hero 模式现在对所有页面生效** —— 原先只对首页生效，文章页与其他页面仍是普通
  全屏横幅。现在只要「全屏 + Hero」两处设置命中，所有页面都是固定整屏壁纸 +
  滚动虚化 + 标题渐隐
- **Hero 下壁纸被下推、上方留白** —— 基础规则给壁纸带了 30vh 的取景偏移
  （横幅模式下用于调整纵向取景），首页原本有兜底归零；Hero 改全局后非首页没有这层
  兜底，壁纸被整体下推约 270px、上方露出一条页面底色。已在 Hero 规则里归零
- **Hero 下自动关闭波浪** —— 波浪锚定在壁纸底边，而 Hero 的壁纸是固定整屏背景、
  没有可贴的边；现在 Hero 下整块隐藏波浪，显示设置面板里的「波浪」开关也同步收起
  （波浪开关只在「横幅」与「全屏 + 经典」下出现）
- **首页向下箭头在浅色壁纸上看不清** —— 保持原有的大箭头造型不加底衬，白色由 85%
  加深到 95%，投影改为两层（近处窄而深勾边缘、外面宽而淡压对比）

### 变更

- **归属表述改为「基础样式取自 Fuwari，移植到 Halo，按 Firefly 的美学补齐外观与交互定制」**，
  同步更新 README / 主题简介 / CHANGELOG / 贡献指南 / Release 说明；各上游项目的版权声明
  仍完整保留在 [LICENSE](./LICENSE)
- 主题自身的占位文本与内部标识统一为 `Onlynn` / `onlynn-`：页脚与日志里的站点名占位、
  控制台横幅、日志前缀，以及访客端的本地记录键（欢迎弹窗关闭标记、公告关闭标记、
  点赞、瞬间点赞）——后者改名会让访客端旧记录失效一次，属预期
  （`ethereal-admonition` 是外部渲染器产出的自定义元素名，作为样式契约保留）

---

## v1.0.2

### 新增

- **打赏支持页**（模板「打赏支持」，建议别名 `sponsor`）—— 引导文案 + 打赏方式 + 打赏列表
  三块：
  - 打赏方式每项一张卡片，**填了收款码就展示二维码，只填链接就显示「前往打赏」按钮**
    （两者都填时优先二维码）；两样都没填的项不出现，一个都没填时显示空态提示而不是空白网格
  - 打赏列表每行是名称 / 金额 / 日期 / 头像，头像留空用名称首字；可整块关掉
  - 图标留空按名称自动匹配（微信 / 支付宝用品牌图标、含「发电」用捧心、ko-fi 用咖啡杯、其余用爱心）
  - 纯服务端渲染，没有配套脚本；三块内容全部在「主题设置 → 扩展页面 → 打赏支持页」维护

### 修复

- **文章目录编号跳号** —— 目录只给最浅一层标题显示序号，但序号取的是「全部标题的下标」，
  正文里一旦夹了 h3，顶层编号就会跳（1、2、3 之后直接到 8）。改为顶层标题单独计数

### 变更

- 打赏页建好后，把地址填进「样式设置 → 首页社交链接 → 打赏页地址」，首页副标题下方即出现打赏入口

---

## v1.0.1

### 新增

- **评论弹幕** —— 把页面已有的评论渲染成从右向左滚动的胶囊弹幕，数据走 Halo 的
  评论公开 API（不抓评论区 DOM）。胶囊跟随主题色、亮暗两套，站长本人的评论单独
  高亮；悬停暂停、点击滚到评论区。留言板放在「留言板」欢迎卡片内（欢迎语下方，
  一条虚线分隔），文章页独立成卡。后台「样式设置 → 评论弹幕」有 11 项可配，
  访客也能在显示设置面板里自行开关

### 修复

- **评论弹幕不再依赖 `haloCommentEnabled`** —— 该变量只有在评论插件实现了
  `CommentWidget` 扩展点时才为真，自带 UI 的评论插件不一定满足；依赖它会让弹幕
  随着评论区的缺席一起不显示。现在只要总开关打开且页面命中「显示范围」就会渲染
- **幕布显隐不再依赖脚本版本** —— 收起状态由幕布自身的 `hidden` 推导（CSS
  `:has()`），即使浏览器或 CDN 还留着旧版 `danmaku.js`，槽位也能跟着显示；
  此前把 `hidden` 挂在槽位上，旧脚本只会解开幕布、揭不开槽位，表现是弹幕完全不显示
- **页脚站点运行时间** —— 条件原先挂在 Astro 的 `Fragment` 上被静默丢弃，
  后台关闭开关后页脚照样显示；条件移到元素上，未开启时连脚本也不下载
- **公告小组件** —— 纯文本公告保留换行（此前多行会被折成一行）

### 变更

- 默认标题改为「楠香一隅，自语成章」，副标题改为「✦在代码与文字间，寻找生活的回响✦」

---

## v1.0.0

首个公开版本。相对于基础样式 Fuwari 的主要变更如下。

### 新增 · 布局与外观

- **Hero 全屏布局** —— 全屏壁纸下可选「经典 / Hero」两种排布：Hero 模式首屏固定在
  视口，内容滚动上推，标题随滚动淡出、壁纸逐步虚化
- **侧栏位置** —— 部件栏可在左 / 右之间切换：两栏布局下与内容对调，三栏布局下与右侧栏
  对调，内容始终居中
- **卡片边框** —— 给卡片与导航栏加一道细描边与轻投影
- **卡片跟随主题色** —— 卡片底色跟随主题色相
- **渐变过渡** —— 壁纸与页面背景衔接处加一层渐变，消除生硬接缝

### 新增 · 动效与特效

- **樱花特效** —— 整屏飘落的花瓣。花瓣用离屏 canvas 程序化绘制，不打包任何来历不明的
  图片素材；片数可配、上限 100，尊重 `prefers-reduced-motion`，页面不可见时暂停
- **移动端底部导航** —— 手机端固定底栏，条目（首页 / 归档 / 分类 / 标签 / 搜索 / 顶部）
  逐项可配；启用后页面自动留白，浮动按钮同步上移避让

### 新增 · 阅读体验

- **沉浸阅读** —— 一键收起导航栏、两侧栏、目录与浮动按钮，正文收成单栏居中并限制行宽；
  右上角常驻出口按钮与 `Esc` 都能退出，状态跨页保持
- **字数与阅读时长** —— 中日韩字符按「字」计、其余按词计，阅读速度取中文 400 字/分钟、
  西文 200 词/分钟
- **相关推荐** —— 按同分类推荐，无分类时退回同标签；自身滤除，不足两篇时整块不显示

### 新增 · 代码块

外观整体重做以对齐 Firefly：灰底标题栏 + 三枚低透明度圆点、语言胶囊（悬停让位给复制
按钮）、行号、长代码块折叠、一键复制。语法高亮由 Shiki 插件提供，主题只负责外框。

### 新增 · 页面与组件

- **RSS 订阅页** —— 订阅地址（一键复制 + 结果提示）、推荐阅读器、最新文章、RSS 说明
- **写作热力图** 侧栏组件 —— 近半年发文密度，格子跟随主题色
- **首页社交链接行** —— GitHub / 邮箱 / 打赏 / RSS，每一项填了才显示
- **页脚站点运行时间** —— 「本站已运行 x 天 x 小时 x 分钟 x 秒」，秒数实时走动

### 调整

- **显示设置面板**由两个标签页扩为**外观 / 壁纸 / 特效**三个
- 面板配置统一收进「**样式设置 → 显示设置面板**」：一个总开关 + 十四个逐项开关控制
  访客权限，各项默认值集中配置；总开关关闭时前台入口整体隐藏，所有访客看到同一套样式
- **代码块 / 面板 / 壁纸**等区域的默认值改为**服务端渲染**，避免访客开关关闭时元素掉回
  CSS 原始默认、站长配置失效
- 默认主题色相调整为 300
- 仓库地址、作者信息与文档全面更名，去除上游遗留的个人链接

### 修复

- **Hero 模式横屏下首屏壁纸缺失** —— 经典全屏靠 `top` 与 `translate` 相抵定位，换固定
  定位后漏掉归零，壁纸被整体下推一个延伸量
- **Hero 模式波浪消失** —— 改为固定定位并跟随标题一起淡出
- **底部出现两条线** —— 插件在代码块底部留了 1rem 外边距，其投影与外框底边形成双线
- **代码块悬停时露出插件的语言文字** —— 用常驻不透明的槽位盖住插件自带的徽章与按钮
- **RSS 页复制按钮显示「复制失败」** —— `writeText` 成功时 resolve 的是 `undefined`，
  被当成失败处理
- 修复访客开关关闭后侧栏位置 / 全屏布局 / 卡片边框 / 跟随主题色 / 渐变默认值失效
- 修复搜索面板点击外部关闭的白名单未包含移动端底栏搜索按钮
