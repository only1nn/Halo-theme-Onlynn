---
import WidgetLayout from "./WidgetLayout.astro";

interface Props {
  class?: string;
  style?: string;
}

const className = Astro.props.class;
const style = Astro.props.style;
const COLLAPSED_HEIGHT = "7.5rem";
const COLLAPSE_THRESHOLD = 5;
---

<div
  class="contents"
  th:with={`categories = \${categoryFinder.listAll()}, isCollapsed = \${#lists.size(categories) >= ${COLLAPSE_THRESHOLD}}`}
>
  <WidgetLayout
    name="分类"
    nameKey="widget.categories.title"
    id="categories"
    collapseExpression="isCollapsed"
    collapsedHeight={COLLAPSED_HEIGHT}
    class={className}
    style={style}
  >
    <div>
      <!-- 分类链接：可见文本即分类名，作为天然可访问名；
           原 th:aria-label（“查看 X 分类下的文章”）不以可见文本开头，
           触发 WCAG 2.5.3 Label in Name 违规，删除后由可见文本自然命名 -->
      <a
        th:each="category : ${categories}"
        th:href="@{${category.status.permalink}}"
        class="group block"
      >
        <div
          class="h-10 rounded-lg px-3 transition-all group-hover:bg-(--btn-plain-bg-hover) group-hover:pl-4 group-active:bg-(--btn-plain-bg-active)"
        >
          <div class="relative mr-2 flex h-full items-center justify-between">
            <span
              class="overflow-hidden overflow-ellipsis whitespace-nowrap text-left text-neutral-700 transition group-hover:text-(--primary) dark:text-neutral-300 dark:group-hover:text-(--primary)"
              th:text="${category.spec.displayName}">Category</span
            >
            <span
              class="ml-3 flex h-7 min-w-8 items-center justify-center rounded-lg bg-(--btn-regular-bg) px-2 text-sm font-bold text-(--btn-content) transition dark:bg-(--primary) dark:text-(--deep-text)"
              th:text="${category.postCount ?: 0}">0</span
            >
          </div>
        </div>
      </a>
    </div>
  </WidgetLayout>
</div>
