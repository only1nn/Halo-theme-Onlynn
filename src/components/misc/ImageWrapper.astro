---
interface Props {
  id?: string;
  src: string;
  class?: string;
  alt?: string;
  position?: string;
  loading?: "lazy" | "eager";
  fetchpriority?: "high" | "low" | "auto";
  /** 可选：Thymeleaf 表达式文本（如 MainGridLayout 传入的 rawSrcset 变量引用），原样输出到 th:srcset */
  srcset?: string;
  /** 可选：sizes 字面量，如 "100vw"（全宽图用，见 MainGridLayout Banner） */
  sizes?: string;
}

const {
  id,
  src,
  alt = "",
  position = "center",
  loading = "lazy",
  fetchpriority = "auto",
  srcset,
  sizes,
} = Astro.props;
const className = Astro.props.class;
const imageStyle = `object-position: ${position}`;
---

<div id={id} class:list={[className, "overflow-hidden relative"]}>
  <div
    class="pointer-events-none absolute inset-0 bg-opacity-50 transition dark:bg-black/10"
  >
  </div>
  <img
    th:src={src}
    th:srcset={srcset}
    sizes={sizes}
    th:alt={alt}
    th:style={`|object-position: ${position}|`}
    loading={loading}
    fetchpriority={fetchpriority}
    decoding="async"
    class="h-full w-full object-cover"
    style={imageStyle}
    alt=""
  />
</div>
