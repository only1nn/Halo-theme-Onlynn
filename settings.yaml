apiVersion: v1alpha1
kind: Setting
metadata:
  name: onlynn-setting
spec:
  forms:
    - group: layout
      label: 布局设置
      formSchema:
        - $formkit: group
          name: pageLayout
          label: 页面布局
          children:
            - $formkit: select
              name: layoutMode
              id: layoutMode
              label: 页面布局样式
              value: three-column
              help: "选择页面的布局模式：两栏（仅左侧边栏）或三栏（左侧 + 右侧边栏）"
              options:
                - label: 两栏布局
                  value: two-column
                - label: 三栏布局
                  value: three-column
            - $formkit: switch
              name: categoryBar
              id: categoryBar_enable
              key: categoryBar_enable
              label: 启用分类导航栏
              value: false
              help: "开启后将在内容区上方显示分类导航栏，包含首页、归档、文章分类及全部分类入口"
        - $formkit: group
          name: mobileBottomBar
          label: 移动端底部导航
          value:
            enable: false
            home: true
            archives: true
            categories: true
            tags: false
            search: true
            top: true
          children:
            - $formkit: switch
              name: enable
              label: 启用
              value: false
              help: "手机端固定在屏幕底部的导航栏；平板与桌面端不显示。启用后页面会自动留出底部空间，避免遮挡页脚"
            - $formkit: checkbox
              name: home
              label: 首页
              value: true
            - $formkit: checkbox
              name: archives
              label: 归档
              value: true
            - $formkit: checkbox
              name: categories
              label: 分类
              value: true
            - $formkit: checkbox
              name: tags
              label: 标签
              value: false
            - $formkit: checkbox
              name: search
              label: 搜索
              value: true
            - $formkit: checkbox
              name: top
              label: 回到顶部
              value: true
        - $formkit: group
          name: postList
          label: 文章卡片布局
          value:
            defaultMode: list
            coverPosition: right
            descriptionLines: 2
            grid:
              masonry: false
              coverFullWidth: false
              coverAutoHeight: true
          children:
            - $formkit: select
              name: coverPosition
              id: postList_coverPosition
              key: postList_coverPosition
              if: "$get(postList_defaultMode).value === 'list'"
              label: 列表模式封面位置
              value: right
              help: "仅列表布局生效：封面显示在卡片右侧或左侧"
              options:
                - label: 右侧
                  value: right
                - label: 左侧
                  value: left
            - $formkit: number
              name: descriptionLines
              label: 简介显示行数
              value: 2
              min: 0
              max: 6
              step: 1
              help: "文章简介显示行数，设为 0 则不截断"
            - $formkit: group
              name: grid
              id: postList_grid
              key: postList_grid
              if: "$get(postList_defaultMode).value === 'grid'"
              label: 网格设置
              help: "以下设置仅网格布局生效"
              children:
                - $formkit: switch
                  name: coverFullWidth
                  key: postList_grid_coverFullWidth
                  label: 封面贴边
                  value: false
                  help: "开启后封面撑满卡片顶部贴边（仅上两角圆角）；关闭则封面按内边距内缩，四角圆角"
                # 注：瀑布流下该开关隐藏（if），隐藏字段不持久化——开启再关闭瀑布流后
                # 会恢复为默认 true。瀑布流本就不参与等高，此重置无实际影响，可接受
                - $formkit: switch
                  name: coverAutoHeight
                  key: postList_grid_coverAutoHeight
                  if: "$get(postList_grid_masonry).value !== true"
                  label: 封面高度自适应
                  value: true
                  help: "开启后封面自动拉伸填满卡片剩余高度，等高网格下同排卡片封面等高、摘要下方不留白；关闭后封面固定 2:1 高度，摘要下方留白（仅等高网格生效，瀑布流下自动隐藏）"
        - $formkit: group
          name: mobileMenu
          label: 菜单栏设置
          value:
            style: accordion
          children:
            - $formkit: attachment
              name: logo
              label: 菜单栏 Logo
              value: ""
              help: "自定义菜单栏左侧 Logo 图片，留空使用主题默认图标。建议上传透明背景图片（高度约 28px），会等比缩放显示"
            - $formkit: switch
              name: showTitle
              id: mobileMenu_showTitle
              key: mobileMenu_showTitle
              label: 显示网站名
              value: true
              help: "开启后在 Logo 右侧显示网站名文字；关闭后菜单栏只显示 Logo（图片或默认图标），仅隐藏菜单栏处的网站名，不影响页面其它位置的站点标题"
            - $formkit: menuSelect
              id: menu
              name: menu
              label: 菜单
              value: ""
              help: "选择导航栏展示的菜单，留空使用系统主菜单"
            - $formkit: select
              name: style
              id: mobileMenu_style
              key: mobileMenu_style
              label: 移动端菜单样式
              value: accordion
              help: "手风琴：从菜单栏下方展开下拉面板，带子菜单手风琴折叠；抽屉：从右侧滑出的圆角卡片，子菜单平铺展示"
              options:
                - label: 手风琴样式
                  value: accordion
                - label: 抽屉样式
                  value: drawer
            - $formkit: switch
              name: navbarFixed
              id: navbarFixed
              label: 固定菜单栏
              value: false
              help: "开启后桌面端菜单栏始终固定在页面顶部，滚动页面时不会自动收起（移动端默认始终固定）"
            - $formkit: switch
              name: enable_change_language
              id: enable_change_language
              label: 语言切换
              value: true
              help: "开启后在菜单栏显示语言切换按钮，访客可切换简体中文 / 繁體中文 / English；关闭则按「主题语言」的默认语言显示。注：切换会同步后台界面语言"
            - $formkit: switch
              name: enable_change_color_scheme
              id: enable_change_color_scheme
              label: 配色（深浅色）切换
              value: true
              help: "开启后在菜单栏显示明暗配色切换按钮，访客可切换深浅色；关闭后隐藏按钮，按「配色方案」中的默认配色显示"
        - $formkit: group
          name: floatingButtons
          label: 浮动导航按钮
          value:
            enable_back_to_top: true
            enable_back_to_home: true
            enable_back_to_comment: true
            enable_toc: true
          children:
            - $formkit: switch
              name: enable_back_to_top
              label: 回到顶部
              value: true
              help: "显示右下角回到顶部按钮"
            - $formkit: switch
              name: enable_back_to_home
              label: 回到首页
              value: true
              help: "显示右下角回到首页按钮"
            - $formkit: switch
              name: enable_back_to_comment
              label: 跳转评论
              value: true
              help: "显示右下角跳转到评论区按钮"
            - $formkit: switch
              name: enable_toc
              label: 目录按钮
              value: true
              help: "页面无可见目录（两栏悬浮目录 / 三栏右侧栏目录）时，显示右下角目录按钮，点击呼出目录弹窗，主要面向移动端与平板端"
            - $formkit: array
              name: customButtons
              label: 自定义按钮
              help: "自定义浮动按钮，按钮将显示在默认按钮上方"
              children:
                - $formkit: text
                  name: name
                  label: 按钮名称
                  validation: required
                - $formkit: iconify
                  name: icon
                  label: 图标
                  format: svg
                - $formkit: url
                  name: url
                  label: 跳转链接
                  validation: required
        - $formkit: group
          name: welcome
          label: 欢迎弹窗
          value:
            enable: false
            position: bottom-right
            title: "欢迎来到我的博客！"
            template: "来自 {location} 的小伙伴，很高兴遇见你！"
            fallbackLocation: "未知星球"
          children:
            - $formkit: switch
              name: enable
              id: welcome_enable
              key: welcome_enable
              label: 启用
              value: false
              help: "开启后访客进入网站时显示欢迎弹窗；访客关闭后本次会话不再弹出。IP 定位复用天气小组件配置的腾讯位置服务 Key，在侧边栏设置中点击「天气」小组件,配置 Key 后生效。"
            - $formkit: select
              name: position
              id: welcome_position
              key: welcome_position
              if: "$get(welcome_enable).value"
              label: 弹窗位置
              value: bottom-right
              help: "电脑端弹窗显示在所选角落；移动端固定显示在底部居中"
              options:
                - label: 左上角
                  value: top-left
                - label: 右上角
                  value: top-right
                - label: 左下角
                  value: bottom-left
                - label: 右下角
                  value: bottom-right
            - $formkit: text
              name: title
              if: "$get(welcome_enable).value"
              label: 欢迎标题
              value: "欢迎来到我的博客！"
              help: "弹窗标题文字"
            - $formkit: textarea
              name: template
              if: "$get(welcome_enable).value"
              label: 欢迎语模板
              value: "来自 {location} 的小伙伴，很高兴遇见你！"
              help: "弹窗正文，{location} 会替换为访客 IP 定位（如：北京市）；未配置 Key 或定位失败时替换为下方的降级文案"
            - $formkit: text
              name: fallbackLocation
              if: "$get(welcome_enable).value"
              label: 定位失败文案
              value: "未知星球"
              help: "未配置腾讯位置服务 Key 或定位失败时，替换 {location} 的地点文字（请填写地点名词，如：未知星球）。模板为「来自 {location} 的小伙伴」时，将显示「来自未知星球的小伙伴」"
    - group: style
      label: 样式设置
      formSchema:
        - $formkit: group
          name: displayPanel
          label: 显示设置面板
          help: "这里集中配置导航栏「显示设置」面板的一切：上半部分是访客能改哪些项，下半部分是各项的默认值。访客的临时改动只存在他自己的浏览器里；要「所有人看到的都是你配的样子」，把总开关关掉即可。"
          children:
            - $formkit: group
              name: visitorStyle
              label: 访客可调样式
              value:
                enable: true
                hue: true
                postListLayout: true
                cardStyle: true
                wallpaperMode: true
                wallpaperSettings: true
                transparent: true
                fullscreenLayout: true
                gradient: true
                carousel: true
                cardBorder: true
                cardFollowTheme: true
                sakura: true
                danmaku: true
                cursorStyle: true
                cursorFx: true
                sidebarPosition: true
              children:
                - $formkit: switch
                  name: enable
                  id: visitorStyle_enable
                  key: visitorStyle_enable
                  label: 允许访客切换样式
                  value: true
                  help: "总开关：开启后导航栏显示「显示设置」按钮，访客可切换下列样式；关闭后按钮整体隐藏，访客只能看到你在后台配置好的样式。各项默认值的位置：主题色相在「主题颜色」，文章布局 / 瀑布流在「文章卡片布局」，侧栏位置在「页面布局」，卡片与壁纸相关项在「样式开关」与「Banner 布局」，首页壁纸标题在「标题与副标题」，樱花在「樱花特效」"
                - $formkit: switch
                  name: hue
                  id: visitorStyle_hue
                  key: visitorStyle_hue
                  if: "$get(visitorStyle_enable).value"
                  label: 主题色相切换
                  value: true
                  help: "访客可在面板中调节主题色相；关闭则隐藏色相选择器，固定为主题颜色中设置的色调"
                - $formkit: switch
                  name: postListLayout
                  id: visitorStyle_postListLayout
                  key: visitorStyle_postListLayout
                  if: "$get(visitorStyle_enable).value"
                  label: 文章布局切换
                  value: true
                  help: "访客可在列表 / 网格布局间切换，后台「文章卡片布局」的默认模式仍作为初始值"
                - $formkit: switch
                  name: cardStyle
                  id: visitorStyle_cardStyle
                  key: visitorStyle_cardStyle
                  if: "$get(visitorStyle_enable).value"
                  label: 卡片样式切换
                  value: true
                  help: "访客可开关卡片悬浮效果、高级材质（磨砂）与瀑布流，后台「样式开关」的值仍作为默认"
                - $formkit: switch
                  name: wallpaperMode
                  id: visitorStyle_wallpaperMode
                  key: visitorStyle_wallpaperMode
                  if: "$get(visitorStyle_enable).value"
                  label: 壁纸模式切换
                  value: true
                  help: "访客可在纯色背景 / 横幅 / 全屏 / 全屏透明四种模式间切换，后台「横幅布局」的显示模式仍作为默认"
                - $formkit: switch
                  name: wallpaperSettings
                  id: visitorStyle_wallpaperSettings
                  key: visitorStyle_wallpaperSettings
                  if: "$get(visitorStyle_enable).value"
                  label: 壁纸设置切换
                  value: true
                  help: "横幅 / 全屏模式下，访客可开关横幅底部波浪动效；后台「样式开关」的波浪设置仍作为默认，后台关闭时前台不显示该开关"
                - $formkit: switch
                  name: cardBorder
                  id: visitorStyle_cardBorder
                  key: visitorStyle_cardBorder
                  if: "$get(visitorStyle_enable).value"
                  label: 卡片边框切换
                  value: true
                  help: "访客可开关卡片描边与投影"
                - $formkit: switch
                  name: sakura
                  id: visitorStyle_sakura
                  key: visitorStyle_sakura
                  if: "$get(visitorStyle_enable).value"
                  label: 樱花特效切换
                  value: true
                  help: "访客可开关飘落的樱花；后台「樱花特效」关闭时前台不显示该开关"
                - $formkit: switch
                  name: danmaku
                  id: visitorStyle_danmaku
                  key: visitorStyle_danmaku
                  if: "$get(visitorStyle_enable).value"
                  label: 弹幕留言切换
                  value: true
                  help: "访客可开关评论弹幕；后台「弹幕留言」关闭时前台不显示该开关"
                - $formkit: switch
                  name: cursorStyle
                  id: visitorStyle_cursorStyle
                  key: visitorStyle_cursorStyle
                  if: "$get(visitorStyle_enable).value"
                  label: 自定义光标切换
                  value: true
                  help: "访客可开关自定义光标；后台「鼠标风格」关闭时前台不显示该开关"
                - $formkit: switch
                  name: cursorFx
                  id: visitorStyle_cursorFx
                  key: visitorStyle_cursorFx
                  if: "$get(visitorStyle_enable).value"
                  label: 鼠标特效切换
                  value: true
                  help: "访客可开关鼠标移动 / 点击特效；后台「鼠标特效」关闭时前台不显示该开关"
                - $formkit: switch
                  name: cardFollowTheme
                  id: visitorStyle_cardFollowTheme
                  key: visitorStyle_cardFollowTheme
                  if: "$get(visitorStyle_enable).value"
                  label: 卡片跟随主题色切换
                  value: true
                  help: "访客可切换卡片底色是否跟随主题色"
                - $formkit: switch
                  name: sidebarPosition
                  id: visitorStyle_sidebarPosition
                  key: visitorStyle_sidebarPosition
                  if: "$get(visitorStyle_enable).value"
                  label: 侧栏位置切换
                  value: true
                  help: "访客可把部件栏在左 / 右之间切换；后台「侧栏位置」仍作为默认"
                - $formkit: switch
                  name: carousel
                  id: visitorStyle_carousel
                  key: visitorStyle_carousel
                  if: "$get(visitorStyle_enable).value"
                  label: 壁纸轮播切换
                  value: true
                  help: "仅当 Banner 采用轮播模式时显示，访客可暂停 / 恢复壁纸自动轮播；关闭后手动圆点仍可切图"
                - $formkit: switch
                  name: gradient
                  id: visitorStyle_gradient
                  key: visitorStyle_gradient
                  if: "$get(visitorStyle_enable).value"
                  label: 渐变过渡切换
                  value: true
                  help: "访客可开关壁纸底部的渐变过渡；后台「样式开关」的值仍作为默认，后台关闭时前台不显示该开关"
                - $formkit: switch
                  name: fullscreenLayout
                  id: visitorStyle_fullscreenLayout
                  key: visitorStyle_fullscreenLayout
                  if: "$get(visitorStyle_enable).value"
                  label: 全屏布局切换
                  value: true
                  help: "仅全屏模式下显示，访客可在经典 / Hero 布局间切换；后台「Banner 布局」的全屏布局仍作为默认"
                - $formkit: switch
                  name: transparent
                  id: visitorStyle_transparent
                  key: visitorStyle_transparent
                  if: "$get(visitorStyle_enable).value"
                  label: 透明设置调节
                  value: true
                  help: "仅横幅「全屏透明」模式下显示，访客可调节壁纸透明度 / 模糊度 / 卡片透明度"
            - $formkit: group
              name: wallpaperDefaults
              label: 壁纸默认值
              value:
                displayMode: banner
                fullscreenLayout: classic
                wallpaperOpacity: 0.8
                wallpaperBlur: 10
                cardOpacity: 0.6
              children:
                - $formkit: select
                  name: displayMode
                  id: banner_displayMode
                  label: 模式
                  value: banner
                  help: "关闭/纯色模式：不显示横幅，页面为纯色背景；横幅模式：非首页显示 35vh 横幅、首页延伸至 65vh；全屏模式：非首页同横幅模式，首页 banner 占满整屏（100vh）；全屏透明：无横幅、整屏壁纸作为页面背景，配合「高级材质」使内容与组件卡片磨砂透明"
                  options:
                    - label: 关闭/纯色模式
                      value: disabled
                    - label: 横幅模式
                      value: banner
                    - label: 全屏模式
                      value: fullscreen
                    - label: 全屏透明
                      value: transparent
                - $formkit: select
                  name: fullscreenLayout
                  id: banner_fullscreenLayout
                  key: banner_fullscreenLayout
                  if: "$get(banner_displayMode).value === 'fullscreen'"
                  label: 全屏布局
                  value: classic
                  help: "仅全屏模式生效。经典：壁纸随页面一起滚动；Hero：壁纸固定为整屏背景，内容从第二屏开始覆盖其上，滚动时标题渐隐、壁纸逐步虚化"
                  options:
                    - label: 经典模式
                      value: classic
                    - label: Hero 模式
                      value: hero
                - $formkit: number
                  name: heroInnerBlur
                  id: banner_heroInnerBlur
                  key: banner_heroInnerBlur
                  if: "$get(banner_displayMode).value === 'fullscreen'"
                  label: Hero 内页背景虚化
                  min: 0
                  max: 24
                  step: 1
                  value: 8
                  help: "仅全屏 + Hero 生效。首页保持清晰（滚动时逐步虚化），其余页面的壁纸按此值常驻虚化，让浮在壁纸上的内容更清楚；填 0 表示不虚化"
                - $formkit: number
                  name: wallpaperOpacity
                  id: banner_transparent_wallpaperOpacity
                  key: banner_transparent_wallpaperOpacity
                  if: "$get(banner_displayMode).value === 'transparent'"
                  label: 壁纸透明度
                  min: 0.3
                  max: 1
                  step: 0.05
                  value: 0.8
                  validation: "min:0.3|max:1"
                  help: "壁纸整体不透明度（0.3~1，默认 0.8），调低后露出页面底色"
                - $formkit: number
                  name: wallpaperBlur
                  id: banner_transparent_wallpaperBlur
                  key: banner_transparent_wallpaperBlur
                  if: "$get(banner_displayMode).value === 'transparent'"
                  label: 背景模糊度
                  min: 0
                  max: 24
                  step: 1
                  value: 10
                  validation: "min:0|max:24"
                  help: "壁纸背景模糊强度（0~24 px，默认 10），值越大越柔和；导航栏与悬浮按钮糊到的内容随之改变"
                - $formkit: number
                  name: cardOpacity
                  id: banner_transparent_cardOpacity
                  key: banner_transparent_cardOpacity
                  if: "$get(banner_displayMode).value === 'transparent'"
                  label: 卡片透明度
                  min: 0.3
                  max: 1
                  step: 0.05
                  value: 0.6
                  validation: "min:0.3|max:1"
                  help: "内容/组件卡片、导航栏与悬浮按钮的半透明程度（0.3~1，默认 0.6），需同时开启「高级材质」"
            - $formkit: group
              name: appearanceDefaults
              label: 外观默认值
              help: "面板「外观」页各项的初始值；主题色相在「主题颜色」、卡片四项在「样式开关」"
              children:
                - $formkit: radio
                  name: sidebarPosition
                  id: sidebarPosition
                  label: 侧栏位置
                  value: left
                  options:
                    - label: 靠左
                      value: left
                    - label: 靠右
                      value: right
                  help: "部件栏放在内容左侧还是右侧；两栏布局下与内容对调，三栏布局下与右侧栏对调，内容始终居中"
                - $formkit: select
                  name: postListMode
                  id: postListMode
                  label: 默认布局
                  value: list
                  help: "列表：单列布局，封面显示在卡片侧边；网格：多列布局，封面显示在卡片顶部"
                  options:
                    - label: 列表
                      value: list
                    - label: 网格
                      value: grid
                - $formkit: switch
                  name: postListMasonry
                  id: postListMasonry
                  key: postListMasonry
                  label: 瀑布流
                  value: false
                  help: "开启后网格使用瀑布流（Masonry）布局：卡片高度参差不齐、错落排列，推荐有图无图混合时开启"
            - $formkit: group
              name: themeColor
              label: 主题颜色
              value:
                hue: 300
              children:
                - $formkit: number
                  name: hue
                  min: 0
                  max: 360
                  step: 5
                  label: 色调
                  value: 300
                  help: "主题颜色的默认色调，从0到360。红色：0，蓝绿色：200，青色：250，紫色：300，粉红色：345"
        - $formkit: group
          name: bannerStyle
          label: Banner 样式
          value:
            mode: single
            src: "/themes/onlynn/assets/images/demo-banner.webp"
            carousel:
              images: []
              effect: fade
              dots: true
              preloadCount: 1
              dwellMs: 5000
            position: "center"
            credit:
              enable: false
              text: "© 2026 Onlynn"
              url: ""
          children:
            - $formkit: select
              name: mode
              id: banner_mode
              label: 类型
              value: single
              help: "单图/视频：显示一张图片或视频，按文件类型自动识别；多图轮播：多张静态图片循环切换"
              options:
                - label: 单图/视频
                  value: single
                - label: 多图轮播
                  value: carousel
            - $formkit: attachment
              name: src
              id: banner_src
              key: banner_src
              if: "$get(banner_mode).value === 'single'"
              label: 图片/视频
              value: "/themes/onlynn/assets/images/demo-banner.webp"
              accepts:
                - "image/*"
                - "video/mp4"
                - "video/webm"
              help: "支持图片（含 GIF/WebP/APNG 动图）与视频（MP4/WebM，静音循环播放）"
            - $formkit: switch
              name: showPauseBtn
              id: banner_showPauseBtn
              key: banner_showPauseBtn
              if: "$get(banner_mode).value === 'single'"
              label: 显示暂停按钮
              value: true
              help: "视频模式下在右下角显示播放/暂停按钮；关闭后视频仅自动静音循环播放，无手动控制（移动端同样生效）"
            - $formkit: group
              name: carousel
              id: banner_carousel
              key: banner_carousel
              if: "$get(banner_mode).value === 'carousel'"
              label: 轮播设置
              value:
                images: []
                effect: fade
                dots: true
                preloadCount: 1
                dwellMs: 5000
              children:
                - $formkit: attachment
                  name: images
                  id: carousel_images
                  key: carousel_images
                  label: 轮播图片
                  multiple: true
                  accepts:
                    - "image/*"
                  value: []
                  help: "选择多张静态图片，按顺序循环切换；少于 2 张时按单图显示。支持外部链接，重复链接会自动附加查询参数强制单独请求；随机图接口建议使用无状态服务（uapis.cn 等接口存在同一浏览器会话内反复请求返回同一张图的问题"
                - $formkit: select
                  name: effect
                  label: 切换效果
                  value: fade
                  options:
                    - label: 淡入淡出
                      value: fade
                    - label: 左右滑动
                      value: slide
                - $formkit: switch
                  name: dots
                  label: 显示指示点
                  value: true
                  help: "在右下角显示可点击的指示点，点击可跳转到对应图片"
                - $formkit: number
                  name: preloadCount
                  label: 预加载数量
                  min: 0
                  max: 10
                  step: 1
                  value: 1
                  help: "预加载当前图片之后的 N 张，0 表示不预加载"
                - $formkit: number
                  name: dwellMs
                  label: 停留时长（毫秒）
                  min: 1000
                  max: 60000
                  step: 500
                  value: 5000
                  help: "每张图片停留时长（毫秒），独立配置，不随动画速度档位变化；切换动画时长仍跟随动画速度档位"
            - $formkit: switch
              name: useMobileSrc
              id: banner_useMobileSrc
              label: 移动端独立来源
              value: false
              help: "开启后为移动端（小于 768px）单独设置 banner 视频/图片来源；关闭时两端共用同一来源"
            - $formkit: group
              name: mobile
              id: banner_mobile
              key: banner_mobile
              if: "$get(banner_useMobileSrc).value"
              label: 移动端设置
              value:
                mode: single
                src: ""
                images: []
              children:
                - $formkit: select
                  name: mode
                  id: banner_mobile_mode
                  label: 类型（移动端）
                  value: single
                  help: "移动端展示形态，可与电脑端不同（如电脑端视频、移动端图片）；行为设置（切换效果/指示点/停留时长/位置等）沿用电脑端"
                  options:
                    - label: 单图/视频
                      value: single
                    - label: 多图轮播
                      value: carousel
                - $formkit: attachment
                  name: src
                  id: banner_mobile_src
                  key: banner_mobile_src
                  if: "$get(banner_useMobileSrc).value && $get(banner_mobile_mode).value === 'single'"
                  label: 图片/视频（移动端）
                  value: ""
                  accepts:
                    - "image/*"
                    - "video/mp4"
                    - "video/webm"
                  help: "移动端单图/视频；留空则移动端使用电脑端来源"
                - $formkit: attachment
                  name: images
                  id: banner_mobile_images
                  key: banner_mobile_images
                  if: "$get(banner_useMobileSrc).value && $get(banner_mobile_mode).value === 'carousel'"
                  label: 轮播图片（移动端）
                  multiple: true
                  accepts:
                    - "image/*"
                  value: []
                  help: "移动端轮播图片，按顺序循环切换；留空则移动端使用电脑端来源。外部随机图接口建议使用无状态服务，uapis.cn 等接口存在同会话返回同一张图的问题"
            - $formkit: select
              name: position
              label: 位置
              value: "center"
              options:
                - value: center
                  label: 居中
                - value: top
                  label: 顶部
                - value: bottom
                  label: 底部
            - $formkit: group
              name: credit
              label: 版权信息
              children:
                - $formkit: switch
                  name: enable
                  label: 启用
                  value: false
                  help: "启用版权信息"
                - $formkit: text
                  name: text
                  label: 文字
                  value: "© 2026 Onlynn"
                  help: "显示的文字"
                - $formkit: url
                  name: url
                  label: 链接
                  value:
        - $formkit: group
          name: socialLinks
          label: 首页社交链接
          value:
            enable: false
            github: ""
            email: ""
            rss: "/rss"
            reward: ""
          children:
            - $formkit: switch
              name: enable
              id: socialLinks_enable
              key: socialLinks_enable
              label: 启用
              value: false
              help: "在首页标题 / 副标题下方显示一排社交按钮；下面每一项留空即隐藏该项"
            - $formkit: text
              name: github
              id: socialLinks_github
              key: socialLinks_github
              if: "$get(socialLinks_enable).value"
              label: GitHub 地址
              help: "形如 https://github.com/yourname；留空则隐藏"
            - $formkit: text
              name: email
              id: socialLinks_email
              key: socialLinks_email
              if: "$get(socialLinks_enable).value"
              label: 邮箱
              help: "点击后调用系统邮件客户端；留空则隐藏"
            - $formkit: text
              name: rss
              id: socialLinks_rss
              key: socialLinks_rss
              if: "$get(socialLinks_enable).value"
              label: RSS 订阅地址
              value: /rss
              help: "默认指向主题自带的 RSS 订阅页；填 /rss.xml 之类的外链也可以，留空则隐藏"
            - $formkit: text
              name: reward
              id: socialLinks_reward
              key: socialLinks_reward
              if: "$get(socialLinks_enable).value"
              label: 打赏页地址
              help: "指向你的打赏 / 赞助页面；留空则隐藏"
        - $formkit: group
          name: bannerText
          label: 标题与副标题
          value:
            enable: true
            title: "楠香一隅，自语成章"
            titleFontSize: "3.5rem"
            subtitles: "✦在代码与文字间，寻找生活的回响✦"
            subtitleFontSize: "1.5rem"
            subtitleEffect: "typewriter"
          children:
            - $formkit: switch
              name: enable
              id: bannerText_enable
              label: 启用
              value: true
              help: "在 Banner 上显示标题和副标题"
            - $formkit: text
              name: title
              if: "$get(bannerText_enable).value"
              label: 标题
              value: "楠香一隅，自语成章"
              help: "Banner 中间显示的标题文字"
            - $formkit: text
              name: titleFontSize
              if: "$get(bannerText_enable).value"
              label: 标题字号
              value: "3.5rem"
              help: "标题字体大小，支持 CSS 单位（如 3.5rem、48px）"
            - $formkit: textarea
              name: subtitles
              if: "$get(bannerText_enable).value"
              label: 副标题
              value: "✦在代码与文字间，寻找生活的回响✦"
              help: "每行一条文本，将循环展示"
            - $formkit: text
              name: subtitleFontSize
              if: "$get(bannerText_enable).value"
              label: 副标题字号
              value: "1.5rem"
              help: "副标题字体大小，支持 CSS 单位（如 1.5rem、20px）"
            - $formkit: select
              name: subtitleEffect
              id: bannerText_subtitleEffect
              if: "$get(bannerText_enable).value"
              label: 副标题动效
              value: "typewriter"
              options:
                - value: typewriter
                  label: 打字机效果
                - value: drop
                  label: 文字下坠
            - $formkit: switch
              name: typewriterBackspace
              id: bannerText_backspace
              if: "$get(bannerText_enable).value && $get(bannerText_subtitleEffect).value === 'typewriter'"
              label: 打字机回退效果
              value: true
              help: "打字完成后逐字回退再重新打字，形成循环动画；关闭则打完即停。副标题写了多行时始终逐条循环，此开关只决定「最后一轮打完后要不要接着打」"
        - $formkit: group
          name: colorScheme
          label: 配色方案
          value:
            color_scheme: auto
          children:
            - $formkit: select
              name: color_scheme
              label: 默认配色
              value: auto
              options:
                - label: 跟随系统
                  value: auto
                - label: 深色
                  value: dark
                - label: 浅色
                  value: light
            - $formkit: group
              name: colorSchemeAnimation
              label: 配色切换动画
              value:
                style: fade
                angle: 90
                easing: default
              children:
                - $formkit: select
                  name: style
                  id: style
                  label: 切换动画样式
                  value: fade
                  help: '已知 Mac 平台部分浏览器"圆形扩散"与"擦除"动画显示异常（圆心偏移、边缘残留、模糊丢失），建议使用淡入淡出以获得多平台更佳体验；需开启菜单栏设置中的「配色（深浅色）切换」开关后生效'
                  options:
                    - label: 淡入淡出
                      value: fade
                    - label: 圆形扩散
                      value: circle
                    - label: 擦除
                      value: wipe
                    - label: 无动画
                      value: none
                - $formkit: select
                  name: easing
                  label: 速度曲线
                  value: default
                  help: "过渡动画的缓动曲线：指数缓出扩散感强，回弹更活泼，线性最机械；动画时长随曲线自动匹配（不可单独设置）"
                  options:
                    - label: 默认
                      value: default
                    - label: 线性
                      value: linear
                    - label: 缓入
                      value: ease-in
                    - label: 缓出
                      value: ease-out
                    - label: 缓入缓出
                      value: ease-in-out
                    - label: 指数缓出
                      value: expo-out
                    - label: 回弹
                      value: back-out
                - $formkit: number
                  name: angle
                  id: angle
                  if: "$get(style).value === 'wipe'"
                  label: 擦除角度（度）
                  min: 0
                  max: 360
                  step: 15
                  value: 90
                  help: "仅擦除样式生效：0° 从左到右，90° 从上到下，180° 从右到左；未选擦除样式时隐藏该设置"
        - $formkit: group
          name: language
          label: 主题语言
          value:
            defaultLanguage: auto
          children:
            - $formkit: select
              name: defaultLanguage
              id: defaultLanguage
              key: defaultLanguage
              label: 默认语言
              value: auto
              help: "访客未手动切换时的默认语言：跟随系统按浏览器语言显示；固定为某语言时首次访问即按该语言渲染。注：切换会同步后台界面语言"
              options:
                - label: 跟随系统
                  value: auto
                - label: 简体中文
                  value: zh-CN
                - label: 繁體中文
                  value: zh-TW
                - label: English
                  value: en
        - $formkit: group
          name: styleSwitches
          label: 样式开关
          value:
            # banner_wave 三选：disabled 关闭 / enabled 开启 / desktop_only 移动端关闭（触屏设备不加载动画）
            banner_wave: enabled
            navbar_blur: true
            card_hover_lift: true
            card_border: false
            card_follow_theme: false
            banner_gradient: true
          children:
            # 三选开关（写法与 color_scheme 一致）：关闭 / 开启 / 移动端关闭
            - $formkit: select
              name: banner_wave
              label: 底部波浪效果
              value: enabled
              help: "在 Banner 底部显示波浪过渡效果；移动端关闭指触屏设备不显示动画，可降低性能开销和加载时间"
              options:
                - label: 关闭
                  value: disabled
                - label: 开启
                  value: enabled
                - label: 移动端关闭
                  value: desktop_only
            - $formkit: switch
              name: navbar_blur
              label: 高级材质
              value: true
              help: "启用后导航栏等元素变为半透明磨砂模糊效果；同时开启 banner「全屏透明」模式时，内容与组件卡片也会变为磨砂透明"
            - $formkit: switch
              name: card_hover_lift
              label: 卡片悬浮效果
              value: true
              help: "卡片悬停时轻微上移并显示主题色阴影"
            - $formkit: switch
              name: card_border
              label: 卡片边框
              value: false
              help: "给卡片与导航栏加一道细描边与轻投影，在纯色背景上更有层次"
            - $formkit: switch
              name: card_follow_theme
              label: 卡片跟随主题色
              value: false
              help: "卡片底色混入主题色相，与页面背景形成同色系层次（默认的纯白 / 纯黑更中性）"
            - $formkit: switch
              name: banner_gradient
              label: 渐变过渡
              value: true
              help: "在壁纸底部叠加一层由透明渐变到页面背景色的遮罩，缓和壁纸与内容区的交界；横幅 / 全屏 / 全屏透明模式下生效"
        - $formkit: group
          name: sakura
          label: 樱花特效
          value:
            enable: false
            count: 21
            zIndex: 100
          children:
            - $formkit: switch
              name: enable
              label: 启用
              value: false
              help: "整屏飘落的樱花花瓣，纯装饰动画；开启后访客仍可在显示设置面板里关掉"
            - $formkit: number
              name: count
              label: 花瓣数量
              value: 21
              min: 1
              max: 100
              step: 1
              help: "默认 21 片；数量越多开销越大，上限 100"
            - $formkit: number
              name: zIndex
              label: 层级
              value: 100
              min: 0
              max: 999
              step: 1
              help: "数值越大越靠前；需小于导航栏与面板，建议 100 左右"
        - $formkit: group
          name: danmaku
          label: 弹幕留言
          value:
            enable: false
            scope: guestbook
            speed: 14
            rows: 5
            gap: 24
            opacity: 0.9
            showAvatar: true
            maxItems: 30
            height: 180
            loop: true
            clickAction: scroll
          children:
            - $formkit: switch
              name: enable
              id: danmaku_enable
              label: 启用
              value: false
              help: "把页面上已有的评论渲染成从右向左滚动的弹幕；关闭时连脚本都不下载"
            - $formkit: select
              name: scope
              if: "$get(danmaku_enable).value"
              label: 显示范围
              value: guestbook
              options:
                - label: 仅留言板
                  value: guestbook
                - label: 仅文章页
                  value: post
                - label: 所有带评论的页面
                  value: all
              help: "弹幕幕布出现在评论区上方，只对能取到评论的页面生效"
            - $formkit: number
              name: speed
              if: "$get(danmaku_enable).value"
              label: 滚动基准秒数
              value: 14
              min: 4
              max: 40
              step: 1
              help: "一条弹幕横穿幕布所需秒数；数值越小越快，建议 8–30"
            - $formkit: number
              name: rows
              if: "$get(danmaku_enable).value"
              label: 轨道数
              value: 5
              min: 1
              max: 12
              step: 1
              help: "弹幕分几行滚动；窄屏会自动减半。轨道总高超过幕布高度时会自动下调，不会叠在一起"
            - $formkit: number
              name: gap
              if: "$get(danmaku_enable).value"
              label: 同轨最小间距
              value: 24
              min: 0
              max: 120
              step: 4
              help: "同一条轨道上前后两条弹幕之间至少留出的空隙（像素）"
            - $formkit: number
              name: opacity
              if: "$get(danmaku_enable).value"
              label: 不透明度
              value: 0.9
              min: 0.3
              max: 1
              step: 0.05
              help: "弹幕胶囊的不透明度，0.3–1.0"
            - $formkit: switch
              name: showAvatar
              if: "$get(danmaku_enable).value"
              label: 显示头像
              value: true
              help: "在弹幕里显示评论者头像；没有头像时用昵称首字代替"
            - $formkit: number
              name: maxItems
              if: "$get(danmaku_enable).value"
              label: 最多显示条数
              value: 30
              min: 5
              max: 100
              step: 5
              help: "按时间倒序取最近若干条评论，过多会显得拥挤"
            - $formkit: number
              name: height
              if: "$get(danmaku_enable).value"
              label: 幕布高度
              value: 180
              min: 80
              max: 360
              step: 10
              help: "弹幕区域高度（像素），只需容纳设定的轨道数即可"
            - $formkit: switch
              name: loop
              if: "$get(danmaku_enable).value"
              label: 循环播放
              value: true
              help: "评论播完后重新开始，保持幕布不空着；关闭则播完即停"
            - $formkit: select
              name: clickAction
              if: "$get(danmaku_enable).value"
              label: 点击弹幕
              value: scroll
              options:
                - label: 滚动到评论区
                  value: scroll
                - label: 不响应点击
                  value: none
              help: "评论插件的评论锚点由插件自身决定，无法保证逐条定位，因此默认只滚到评论区"
        - $formkit: group
          name: animationSpeed
          label: 动画速度
          value:
            speedTier: balanced
          children:
            - $formkit: select
              name: speedTier
              id: speedTier
              label: 速度档位
              value: balanced
              help: "舒缓：保留完整错峰入场演出；均衡：当前主题原值；疾速：入场只做轻微位移、内容立即可见（换页时长随之缩短）；自定义：手动逐项设置各动画时长（不涉及主题切换动画，其仍随缓动曲线匹配）"
              options:
                - label: 舒缓
                  value: relaxed
                - label: 均衡
                  value: balanced
                - label: 疾速
                  value: snappy
                - label: 自定义
                  value: custom
            - $formkit: group
              name: speedCustom
              if: "$get(speedTier).value === 'custom'"
              label: 自定义动画时长
              help: "仅在速度档位选择「自定义」时展示；单位毫秒"
              value:
                swup: 200
                entry: 300
                entryStep: 50
                entryMax: 400
                contentDelay: 150
                scroll: 700
                float: 350
                banner: 700
                carouselTransition: 700
              children:
                - $formkit: number
                  name: swup
                  label: 换页退场（ms）
                  min: 0
                  max: 1000
                  step: 10
                  value: 200
                  help: "页面切换时旧内容淡出下移的时长（原 200ms）"
                - $formkit: number
                  name: entry
                  label: 入场动画（ms）
                  min: 0
                  max: 1000
                  step: 10
                  value: 300
                  help: "换页后新内容 fade-in-up 时长（原 300ms）"
                - $formkit: number
                  name: entryStep
                  label: 错峰步长（ms）
                  min: 0
                  max: 200
                  step: 10
                  value: 50
                  help: "列表/组件逐项入场的递增值（原 50ms），0 表示无错峰"
                - $formkit: number
                  name: entryMax
                  label: 错峰上限（ms）
                  min: 0
                  max: 1000
                  step: 10
                  value: 400
                  help: "入场延迟最大值（原 banner-credit 400ms），错峰超过此值被截断"
                - $formkit: number
                  name: contentDelay
                  label: 内容基础延迟（ms）
                  min: 0
                  max: 1000
                  step: 10
                  value: 150
                  help: "内容主区块的入场延迟（原 --content-delay 150ms）"
                - $formkit: number
                  name: scroll
                  label: 导航栏隐藏（ms）
                  min: 0
                  max: 2000
                  step: 50
                  value: 700
                  help: "下滑隐藏导航栏的时长（原 700ms）"
                - $formkit: number
                  name: float
                  label: 浮动按钮位移（ms）
                  min: 0
                  max: 1000
                  step: 10
                  value: 350
                  help: "回顶/首页/评论浮动按钮位移动画时长（原 350ms）"
                - $formkit: number
                  name: banner
                  label: Banner 载入（ms）
                  min: 0
                  max: 2000
                  step: 50
                  value: 700
                  help: "首屏 banner 图载入渐显时长（原 700ms）"
                - $formkit: number
                  name: carouselTransition
                  label: 轮播切换（ms）
                  min: 0
                  max: 2000
                  step: 10
                  value: 700
                  help: "轮播图切换动画时长（原 700ms），随动画速度档位变化"
        - $formkit: group
          name: externalFont
          label: 外部字体
          value:
            enable: false
            family: ""
          children:
            - $formkit: switch
              name: enable
              id: externalFont_enable
              label: 启用外部字体
              value: false
              help: "使用第三方字体替换浏览器默认字体，当前仅支持全局替换"
            - $formkit: attachment
              name: fontFile
              if: "$get(externalFont_enable).value"
              label: 字体资源
              value: ""
              validation: required
              help: "上传字体文件（TTF/OTF/WOFF/WOFF2），或粘贴 Google Fonts 等 CSS 链接地址"
            - $formkit: text
              name: family
              if: "$get(externalFont_enable).value"
              label: font-family 名称
              value: ""
              validation: required
              help: "填写要应用的字体名称"
    - group: sidebar
      label: 侧边栏
      formSchema:
        - $formkit: group
          name: widgetsConfig
          label: 小组件设置
          children:
            - $formkit: array
              name: widgets
              label: 左侧边栏小部件
              help: "在左侧边栏显示的部件，按顺序排列；点击部件进行详细配置"
              value:
                - value: categories
                - value: tag
              children:
                - $formkit: select
                  name: value
                  id: widgets_value
                  label: 部件
                  options:
                    - label: 热门文章
                      value: popular-posts
                    - label: 文章分类
                      value: categories
                    - label: 文章标签
                      value: tag
                    - label: 音乐播放器
                      value: music
                    - label: 一言
                      value: hitokoto
                    - label: 站点统计
                      value: site-stats
                    - label: 写作热力图
                      value: heatmap
                    - label: 天气
                      value: weather
                    - label: 最近日程
                      value: schedule
                    - label: 自定义HTML
                      value: html
                - $formkit: checkbox
                  if: "$value.value === 'site-stats'"
                  name: show_posts
                  key: widget_site_stats_show_posts
                  preserve: true
                  label: 显示文章数
                  value: true
                - $formkit: checkbox
                  if: "$value.value === 'site-stats'"
                  name: show_categories
                  key: widget_site_stats_show_categories
                  preserve: true
                  label: 显示分类数
                  value: true
                - $formkit: checkbox
                  if: "$value.value === 'site-stats'"
                  name: show_tags
                  key: widget_site_stats_show_tags
                  preserve: true
                  label: 显示标签数
                  value: true
                - $formkit: checkbox
                  if: "$value.value === 'site-stats'"
                  name: show_total_words
                  key: widget_site_stats_show_total_words
                  preserve: true
                  label: 显示总字数
                  value: true
                  help: "未安装「API 拓展包」插件时由客户端逐篇统计，文章较多会有额外请求"
                - $formkit: checkbox
                  if: "$value.value === 'site-stats'"
                  name: show_last_activity
                  key: widget_site_stats_show_last_activity
                  preserve: true
                  label: 显示最后活动
                  value: true
                - $formkit: checkbox
                  if: "$value.value === 'site-stats'"
                  name: show_running_days
                  key: widget_site_stats_show_running_days
                  preserve: true
                  label: 显示运行天数
                  value: true
                  help: "需同时在下方选择网站上线日期，未选择则不显示该项"
                - $formkit: checkbox
                  if: "$value.value === 'site-stats'"
                  name: show_visits
                  key: widget_site_stats_show_visits
                  preserve: true
                  label: 显示访问量
                  value: false
                - $formkit: checkbox
                  if: "$value.value === 'site-stats'"
                  name: show_upvotes
                  key: widget_site_stats_show_upvotes
                  preserve: true
                  label: 显示点赞数
                  value: false
                - $formkit: checkbox
                  if: "$value.value === 'site-stats'"
                  name: show_comments
                  key: widget_site_stats_show_comments
                  preserve: true
                  label: 显示评论数
                  value: false
                - $formkit: date
                  if: "$value.value === 'site-stats'"
                  name: site_start_date
                  key: widget_site_stats_site_start_date
                  preserve: true
                  label: 网站上线日期
                  help: "用于计算运行天数，留空则不显示运行天数"
                - $formkit: text
                  if: "$value.value === 'weather'"
                  name: tencent_key
                  key: widget_weather_tencent_key
                  preserve: true
                  label: 腾讯位置服务 Key
                  value: ""
                  help: "WebService API 类型，免费注册：https://lbs.qq.com/dev/console/application/mine；Key 会在前端页面源码中明文出现，建议在腾讯位置服务控制台配置域名白名单。对【账户额度】下的“天气:天气”和“IP定位”分别进行配额分配，确保“调用量分配”和“并发量分配”满足网站需求。"
                - $formkit: text
                  if: "$value.value === 'weather'"
                  name: default_city
                  key: widget_weather_default_city
                  preserve: true
                  label: 默认城市编码
                  value: "110000"
                  help: "IP定位失败时的回退城市，填写行政区划编码如 110000（北京）或 310000（上海）"
                - $formkit: url
                  if: "$value.value === 'hitokoto'"
                  name: hitokoto_api
                  key: widget_hitokoto_api
                  preserve: true
                  label: 一言 API 地址
                  value: ""
                  help: "留空则使用内置默认一言 API（https://v1.hitokoto.cn/）"
                - $formkit: text
                  if: "$value.value === 'hitokoto'"
                  name: fallback_text
                  key: widget_hitokoto_fallback_text
                  preserve: true
                  label: 获取失败文案
                  value: "愿做檐下一株草，不争不抢，自在生长。"
                  help: "一言获取失败时显示的句子"
                - $formkit: text
                  if: "$value.value === 'hitokoto'"
                  name: fallback_source
                  key: widget_hitokoto_fallback_source
                  preserve: true
                  label: 获取失败来源
                  value: "楠香自语"
                  help: "一言获取失败时显示的作者或来源"
                - $formkit: select
                  if: "$value.value === 'music'"
                  name: server
                  key: widget_music_server
                  preserve: true
                  label: 平台
                  value: netease
                  options:
                    - label: 网易云音乐
                      value: netease
                    - label: QQ 音乐
                      value: tencent
                    - label: 酷狗音乐
                      value: kugou
                    - label: 虾米音乐
                      value: xiami
                    - label: 百度音乐
                      value: baidu
                - $formkit: select
                  if: "$value.value === 'music'"
                  name: type
                  key: widget_music_type
                  preserve: true
                  label: 类型
                  value: playlist
                  options:
                    - label: 歌曲
                      value: song
                    - label: 歌单
                      value: playlist
                    - label: 专辑
                      value: album
                    - label: 歌手
                      value: artist
                    - label: 搜索
                      value: search
                - $formkit: text
                  if: "$value.value === 'music'"
                  name: id
                  key: widget_music_id
                  preserve: true
                  label: ID
                  validation: required
                  help: 歌曲 / 歌单 / 专辑 / 歌手 ID，搜索类型时填写关键词
                - $formkit: select
                  if: "$value.value === 'music'"
                  name: play_mode
                  key: widget_music_play_mode
                  preserve: true
                  label: 播放模式
                  value: list
                  options:
                    - label: 列表循环
                      value: list
                    - label: 单曲循环
                      value: one
                    - label: 随机
                      value: random
                - $formkit: number
                  if: "$value.value === 'music'"
                  name: volume
                  key: widget_music_volume
                  preserve: true
                  label: 默认音量
                  min: 0
                  max: 1
                  step: 0.1
                  value: 0.7
                - $formkit: url
                  if: "$value.value === 'music'"
                  name: music_api
                  key: widget_music_api
                  preserve: true
                  label: 自定义 Meting API
                  help: 留空时使用 MetingJS 默认 API，地址可包含 :server、:type、:id、:r 占位符
                - $formkit: number
                  if: "$value.value === 'schedule'"
                  name: limit
                  key: widget_schedule_limit
                  preserve: true
                  label: 显示条数
                  min: 1
                  max: 10
                  value: 3
                  help: "显示最近日程的条数，最多10条，显示过多会导致小组件过高，建议保持默认"
                - $formkit: textarea
                  if: "$value.value === 'html'"
                  name: html
                  key: widget_html_html
                  preserve: true
                  label: HTML
                  value: "<p>自定义HTML</p>"
            - $formkit: array
              name: rightWidgets
              label: 右侧边栏小部件
              help: "仅三栏布局（布局设置中的页面布局）下生效，两栏布局时不显示右侧边栏;点击部件进行详细配置"
              value:
                - value: hitokoto
                - value: site-stats
              children:
                - $formkit: select
                  name: value
                  id: right_widgets_value
                  label: 部件
                  options:
                    - label: 热门文章
                      value: popular-posts
                    - label: 文章分类
                      value: categories
                    - label: 文章标签
                      value: tag
                    - label: 音乐播放器
                      value: music
                    - label: 一言
                      value: hitokoto
                    - label: 站点统计
                      value: site-stats
                    - label: 写作热力图
                      value: heatmap
                    - label: 天气
                      value: weather
                    - label: 最近日程
                      value: schedule
                    - label: 自定义HTML
                      value: html
                - $formkit: checkbox
                  if: "$value.value === 'site-stats'"
                  name: show_posts
                  key: right_widget_site_stats_show_posts
                  preserve: true
                  label: 显示文章数
                  value: true
                - $formkit: checkbox
                  if: "$value.value === 'site-stats'"
                  name: show_categories
                  key: right_widget_site_stats_show_categories
                  preserve: true
                  label: 显示分类数
                  value: true
                - $formkit: checkbox
                  if: "$value.value === 'site-stats'"
                  name: show_tags
                  key: right_widget_site_stats_show_tags
                  preserve: true
                  label: 显示标签数
                  value: true
                - $formkit: checkbox
                  if: "$value.value === 'site-stats'"
                  name: show_total_words
                  key: right_widget_site_stats_show_total_words
                  preserve: true
                  label: 显示总字数
                  value: true
                  help: "未安装「API 拓展包」插件时由客户端逐篇统计，文章较多会有额外请求"
                - $formkit: checkbox
                  if: "$value.value === 'site-stats'"
                  name: show_last_activity
                  key: right_widget_site_stats_show_last_activity
                  preserve: true
                  label: 显示最后活动
                  value: true
                - $formkit: checkbox
                  if: "$value.value === 'site-stats'"
                  name: show_running_days
                  key: right_widget_site_stats_show_running_days
                  preserve: true
                  label: 显示运行天数
                  value: true
                  help: "需同时在下方选择网站上线日期，未选择则不显示该项"
                - $formkit: checkbox
                  if: "$value.value === 'site-stats'"
                  name: show_visits
                  key: right_widget_site_stats_show_visits
                  preserve: true
                  label: 显示访问量
                  value: false
                - $formkit: checkbox
                  if: "$value.value === 'site-stats'"
                  name: show_upvotes
                  key: right_widget_site_stats_show_upvotes
                  preserve: true
                  label: 显示点赞数
                  value: false
                - $formkit: checkbox
                  if: "$value.value === 'site-stats'"
                  name: show_comments
                  key: right_widget_site_stats_show_comments
                  preserve: true
                  label: 显示评论数
                  value: false
                - $formkit: date
                  if: "$value.value === 'site-stats'"
                  name: site_start_date
                  key: right_widget_site_stats_site_start_date
                  preserve: true
                  label: 网站上线日期
                  help: "用于计算运行天数，留空则不显示运行天数"
                - $formkit: text
                  if: "$value.value === 'weather'"
                  name: tencent_key
                  key: right_widget_weather_tencent_key
                  preserve: true
                  label: 腾讯位置服务 Key
                  value: ""
                  help: "WebService API 类型，免费注册：https://lbs.qq.com/dev/console/application/mine；Key 会在前端页面源码中明文出现，建议在腾讯位置服务控制台配置域名白名单。对【账户额度】下的“天气:天气”和“IP定位”分别进行配额分配，确保“调用量分配”和“并发量分配”满足网站需求。"
                - $formkit: text
                  if: "$value.value === 'weather'"
                  name: default_city
                  key: right_widget_weather_default_city
                  preserve: true
                  label: 默认城市编码
                  value: "110000"
                  help: "IP定位失败时的回退城市，填写行政区划编码如 110000（北京）或 310000（上海）"
                - $formkit: url
                  if: "$value.value === 'hitokoto'"
                  name: hitokoto_api
                  key: right_widget_hitokoto_api
                  preserve: true
                  label: 一言 API 地址
                  value: ""
                  help: "留空则使用内置默认一言 API（https://v1.hitokoto.cn/）"
                - $formkit: text
                  if: "$value.value === 'hitokoto'"
                  name: fallback_text
                  key: right_widget_hitokoto_fallback_text
                  preserve: true
                  label: 获取失败文案
                  value: "愿做檐下一株草，不争不抢，自在生长。"
                  help: "一言获取失败时显示的句子"
                - $formkit: text
                  if: "$value.value === 'hitokoto'"
                  name: fallback_source
                  key: right_widget_hitokoto_fallback_source
                  preserve: true
                  label: 获取失败来源
                  value: "楠香自语"
                  help: "一言获取失败时显示的作者或来源"
                - $formkit: select
                  if: "$value.value === 'music'"
                  name: server
                  key: right_widget_music_server
                  preserve: true
                  label: 平台
                  value: netease
                  options:
                    - label: 网易云音乐
                      value: netease
                    - label: QQ 音乐
                      value: tencent
                    - label: 酷狗音乐
                      value: kugou
                    - label: 虾米音乐
                      value: xiami
                    - label: 百度音乐
                      value: baidu
                - $formkit: select
                  if: "$value.value === 'music'"
                  name: type
                  key: right_widget_music_type
                  preserve: true
                  label: 类型
                  value: playlist
                  options:
                    - label: 歌曲
                      value: song
                    - label: 歌单
                      value: playlist
                    - label: 专辑
                      value: album
                    - label: 歌手
                      value: artist
                    - label: 搜索
                      value: search
                - $formkit: text
                  if: "$value.value === 'music'"
                  name: id
                  key: right_widget_music_id
                  preserve: true
                  label: ID
                  validation: required
                  help: 歌曲 / 歌单 / 专辑 / 歌手 ID，搜索类型时填写关键词
                - $formkit: select
                  if: "$value.value === 'music'"
                  name: play_mode
                  key: right_widget_music_play_mode
                  preserve: true
                  label: 播放模式
                  value: list
                  options:
                    - label: 列表循环
                      value: list
                    - label: 单曲循环
                      value: one
                    - label: 随机
                      value: random
                - $formkit: number
                  if: "$value.value === 'music'"
                  name: volume
                  key: right_widget_music_volume
                  preserve: true
                  label: 默认音量
                  min: 0
                  max: 1
                  step: 0.1
                  value: 0.7
                - $formkit: url
                  if: "$value.value === 'music'"
                  name: music_api
                  key: right_widget_music_api
                  preserve: true
                  label: 自定义 Meting API
                  help: 留空时使用 MetingJS 默认 API，地址可包含 :server、:type、:id、:r 占位符
                - $formkit: number
                  if: "$value.value === 'schedule'"
                  name: limit
                  key: right_widget_schedule_limit
                  preserve: true
                  label: 显示条数
                  min: 1
                  max: 10
                  value: 3
                  help: "显示最近日程的条数，最多10条，显示过多会导致小组件过高，建议保持默认"
                - $formkit: textarea
                  if: "$value.value === 'html'"
                  name: html
                  key: right_widget_html_html
                  preserve: true
                  label: HTML
                  value: "<p>自定义HTML</p>"
        - $formkit: group
          name: profile
          label: 个人简介小组件
          value:
            enable_profile: true
            display_position: left
          children:
            - $formkit: switch
              name: enable_profile
              id: enable_profile_switch
              value: true
              label: 启用个人简介小组件
              help: "开启后显示个人简介小组件（位于侧边栏首位，不参与吸顶，随页面滚动）"
            - $formkit: select
              name: display_position
              if: "$get(enable_profile_switch).value"
              label: 个人简介小组件显示位置
              value: left
              help: "选择个人简介小组件显示在左侧还是右侧侧边栏，两栏布局时仅显示在左侧"
              options:
                - label: 左侧边栏
                  value: left
                - label: 右侧边栏
                  value: right
            - $formkit: attachment
              name: avatar
              if: "$get(enable_profile_switch).value"
              value: /themes/onlynn/assets/images/demo-avatar.png
              label: 头像
            - $formkit: text
              name: name
              if: "$get(enable_profile_switch).value"
              label: 昵称
              value: ""
            - $formkit: text
              name: bio
              if: "$get(enable_profile_switch).value"
              label: 简介
              value: "在这里写一句自我介绍。"
            - $formkit: url
              name: url
              if: "$get(enable_profile_switch).value"
              label: 链接(点击跳转登录和后台可使用 /login )
              value: /about
            - $formkit: array
              name: social_media
              if: "$get(enable_profile_switch).value"
              label: 社交媒体
              children:
                - $formkit: iconify
                  name: social_icon
                  label: 社交图标
                  format: svg
                - $formkit: text
                  name: name
                  label: 名称
                  validation: "required"
                - $formkit: text
                  name: url
                  label: 链接
                  validation: "required"
            - $formkit: group
              name: statusSettings
              if: "$get(enable_profile_switch).value"
              label: 在线状态
              help: "昵称右侧的状态表情与展示文案"
              value:
                enable: true
                status: online
                online: ""
                energetic: ""
                emo: ""
                study: ""
                busy: ""
                dnd: ""
                sleep: ""
                away: ""
              children:
                - $formkit: switch
                  name: enable
                  id: status_settings_enable
                  value: true
                  label: 启用在线状态
                  help: "关闭后隐藏昵称右侧的状态表情，并隐藏下方设置项"
                - $formkit: select
                  name: status
                  id: profile_status
                  if: "$get(status_settings_enable).value"
                  label: 当前状态
                  value: online
                  help: "显示在昵称右侧的状态表情，登录管理员账号后也可在前端点击切换"
                  options:
                    - label: 在线中
                      value: online
                    - label: 元气满满
                      value: energetic
                    - label: emo中
                      value: emo
                    - label: 学习中
                      value: study
                    - label: 忙碌中
                      value: busy
                    - label: 请勿打扰
                      value: dnd
                    - label: 睡觉中
                      value: sleep
                    - label: 暂时离开
                      value: away
                - $formkit: text
                  name: online
                  # 关键：互斥 if 条件的同类型字段必须加唯一 key，
                  # 否则 Vue 切换时复用组件实例导致文案值丢失（Halo FormKit 已知坑）
                  key: status_text_online
                  if: "$get(status_settings_enable).value && $get(profile_status).value === 'online'"
                  label: 状态文案
                  value: ""
                  maxlength: 10
                  help: "「在线中」状态的文案，留空使用默认，最多 10 字"
                - $formkit: text
                  name: energetic
                  key: status_text_energetic
                  if: "$get(status_settings_enable).value && $get(profile_status).value === 'energetic'"
                  label: 状态文案
                  value: ""
                  maxlength: 10
                  help: "「元气满满」状态的文案，留空使用默认，最多 10 字"
                - $formkit: text
                  name: emo
                  key: status_text_emo
                  if: "$get(status_settings_enable).value && $get(profile_status).value === 'emo'"
                  label: 状态文案
                  value: ""
                  maxlength: 10
                  help: "「emo中」状态的文案，留空使用默认，最多 10 字"
                - $formkit: text
                  name: study
                  key: status_text_study
                  if: "$get(status_settings_enable).value && $get(profile_status).value === 'study'"
                  label: 状态文案
                  value: ""
                  maxlength: 10
                  help: "「学习中」状态的文案，留空使用默认，最多 10 字"
                - $formkit: text
                  name: busy
                  key: status_text_busy
                  if: "$get(status_settings_enable).value && $get(profile_status).value === 'busy'"
                  label: 状态文案
                  value: ""
                  maxlength: 10
                  help: "「忙碌中」状态的文案，留空使用默认，最多 10 字"
                - $formkit: text
                  name: dnd
                  key: status_text_dnd
                  if: "$get(status_settings_enable).value && $get(profile_status).value === 'dnd'"
                  label: 状态文案
                  value: ""
                  maxlength: 10
                  help: "「请勿打扰」状态的文案，留空使用默认，最多 10 字"
                - $formkit: text
                  name: sleep
                  key: status_text_sleep
                  if: "$get(status_settings_enable).value && $get(profile_status).value === 'sleep'"
                  label: 状态文案
                  value: ""
                  maxlength: 10
                  help: "「睡觉中」状态的文案，留空使用默认，最多 10 字"
                - $formkit: text
                  name: away
                  key: status_text_away
                  if: "$get(status_settings_enable).value && $get(profile_status).value === 'away'"
                  label: 状态文案
                  value: ""
                  maxlength: 10
                  help: "「暂时离开」状态的文案，留空使用默认，最多 10 字"
        - $formkit: group
          name: announcement
          label: 公告小组件
          value:
            enable: false
            position: left
            content: "欢迎来到我的博客！这是一则示例公告。"
            closable: true
            link:
              enable: false
              text: "了解更多"
              url: ""
              external: false
          children:
            - $formkit: switch
              name: enable
              id: announcement_enable
              label: 启用公告
              value: false
              help: "开启后显示公告小组件（侧边栏首位为个人简介小组件时显示在第二位），不参与吸顶，随页面滚动"
            - $formkit: select
              name: position
              if: "$get(announcement_enable).value"
              label: 显示位置
              value: left
              help: "公告仅可显示在左侧边栏或右侧边栏，两栏布局时仅显示在左侧"
              options:
                - label: 左侧边栏
                  value: left
                - label: 右侧边栏
                  value: right
            - $formkit: textarea
              name: content
              if: "$get(announcement_enable).value"
              label: 公告内容
              value: "欢迎来到我的博客！这是一则示例公告。"
            - $formkit: switch
              name: enable_html
              id: announcement_enable_html
              key: announcement_enable_html
              if: "$get(announcement_enable).value"
              label: 内容呈现为HTML
              value: false
              help: "开启后按 HTML 渲染（可写链接、图标、富文本），关闭时按纯文本显示。样式请用 px/rem 固定值；内容不过滤，只填可信 HTML；带脚本时用类选择器而非 id 初始化。"
            - $formkit: number
              name: content_height
              id: announcement_content_height
              key: announcement_content_height
              if: "$get(announcement_enable_html).value"
              label: 内容最大高度(px)
              value: 640
              min: 40
              max: 1024
              help: "内容区高度由 HTML 自身决定，超过此值则在公告内部滚动；留空使用默认 640px"
            - $formkit: switch
              name: closable
              if: "$get(announcement_enable).value"
              label: 允许关闭
              value: true
              help: "开启后访客可点击关闭按钮隐藏公告"
            - $formkit: group
              name: link
              if: "$get(announcement_enable).value"
              label: 链接设置
              children:
                - $formkit: switch
                  name: enable
                  id: announcement_link_enable
                  label: 启用链接
                  value: false
                - $formkit: text
                  name: text
                  if: "$get(announcement_link_enable).value"
                  label: 链接文本
                  value: "了解更多"
                - $formkit: url
                  name: url
                  if: "$get(announcement_link_enable).value"
                  label: 链接地址
                  value: ""
                - $formkit: switch
                  name: external
                  if: "$get(announcement_link_enable).value"
                  label: 外部链接
                  value: false
                  help: "开启后在新标签页打开"
    - group: post
      label: 文章
      formSchema:
        - $formkit: group
          name: license
          label: 版权声明
          value:
            enable: false
            name: CC BY-NC-SA 4.0
            url: https://creativecommons.org/licenses/by-nc-sa/4.0/
          children:
            - $formkit: switch
              name: enable
              id: license_enable
              label: 启用
              value: false
              help: "启用版权声明"
            - $formkit: text
              name: name
              if: "$get(license_enable).value"
              label: 名称
              value: CC BY-NC-SA 4.0
            - $formkit: url
              name: url
              if: "$get(license_enable).value"
              label: 链接
              value: https://creativecommons.org/licenses/by-nc-sa/4.0/
        - $formkit: group
          name: relatedPosts
          label: 相关推荐
          value:
            enable: false
            count: 3
          children:
            - $formkit: switch
              name: enable
              label: 启用
              value: false
              help: "在文章底部、评论区之前显示相关推荐；优先取同分类文章，没有分类时取同标签"
            - $formkit: number
              name: count
              if: "$get(relatedPosts_enable).value"
              label: 显示条数
              value: 3
              min: 1
              max: 6
              help: "最多 6 条；分类里文章不足时按实际数量显示，少于 2 篇时整个区块不显示"
        - $formkit: group
          name: meta
          label: 文章信息
          value:
            wordCount: true
            readingTime: true
            heat: false
          children:
            - $formkit: switch
              name: wordCount
              label: 显示字数
              value: true
              help: "在文章信息栏显示正文字数，由浏览器在打开文章时统计"
            - $formkit: switch
              name: readingTime
              label: 显示阅读时长
              value: true
              help: "按中文 400 字/分钟、西文 200 词/分钟估算，最少 1 分钟"
            - $formkit: switch
              name: heat
              label: 显示文章热度
              value: false
              help: "按 24 + 浏览量×0.1 + 点赞×2 + 评论×3 计算的「℃」热度，分档配色；列表卡片与文章页都会显示"
        - $formkit: group
          name: codeBlock
          label: 代码块
          value:
            languageBadge: true
            collapsibleEnable: true
            collapsibleLineThreshold: 15
            collapsiblePreviewLines: 8
          children:
            - $formkit: switch
              name: languageBadge
              label: 显示语言标签
              value: true
              help: "在代码块右上角显示语言胶囊；鼠标悬停时让位给复制按钮"
            - $formkit: switch
              name: collapsibleEnable
              label: 长代码块自动折叠
              value: true
              help: "超过下面行数的代码块默认折叠，点击「展开」查看全文"
            - $formkit: number
              name: collapsibleLineThreshold
              label: 折叠阈值
              value: 15
              min: 5
              max: 200
              step: 5
              help: "超过这个行数时折叠"
            - $formkit: number
              name: collapsiblePreviewLines
              label: 折叠后显示行数
              value: 8
              min: 3
              max: 50
              step: 1
        - $formkit: group
          name: contentDisplay
          label: 内容显示
          value:
            showCover: true
            content_size: prose-base
            content_theme: prose-gray
          children:
            - $formkit: switch
              name: showCover
              label: 显示封面图
              value: true
              help: "在文章页正文上方显示封面图；关闭后列表/卡片仍正常显示封面，仅文章页内不重复显示"
            - $formkit: select
              name: content_size
              label: 字体大小
              value: prose-base
              options:
                - value: prose-base
                  label: prose-base
                - value: prose-sm
                  label: prose-sm
                - value: prose-lg
                  label: prose-lg
                - value: prose-xl
                  label: prose-xl
                - value: prose-2xl
                  label: prose-2xl
            - $formkit: select
              name: content_theme
              label: 颜色主题
              value: prose-gray
              options:
                - value: prose-gray
                  label: prose-gray
                - value: prose-slate
                  label: prose-slate
                - value: prose-zinc
                  label: prose-zinc
                - value: prose-neutral
                  label: prose-neutral
                - value: prose-stone
                  label: prose-stone
        - $formkit: group
          name: toc
          label: 目录
          value:
            enable_toc: true
            toc_depth: 2
          children:
            - $formkit: switch
              name: enable_toc
              id: toc_enable
              label: 启用目录
              value: true
              help: "启用目录"
            - $formkit: number
              name: toc_depth
              if: "$get(toc_enable).value"
              label: 目录最大深度
              value: 2
              help: "目录的最大深度"
        - $formkit: group
          name: summary
          label: 文章摘要
          value:
            enable_summary: true
            summary_title: "文章摘要"
            summaryEffect: "typewriter"
          children:
            - $formkit: switch
              name: enable_summary
              id: summary_enable
              label: 启用文章摘要
              value: true
              help: "在文章页面封面图下方显示自定义摘要"
            - $formkit: text
              name: summary_title
              if: "$get(summary_enable).value"
              label: 摘要标题
              value: "文章摘要"
              help: "摘要框显示的标题文字，留空则不显示标题"
            - $formkit: select
              name: summaryEffect
              if: "$get(summary_enable).value"
              label: 摘要文字动效
              value: "typewriter"
              options:
                - value: typewriter
                  label: 打字机效果
                - value: drop
                  label: 文字下坠
        - $formkit: group
          name: actionBar
          label: 文章操作栏
          value:
            enable: true
            like: true
            share: true
            reward: true
            readingFocus: true
            rewardSetting:
              title: "如果这篇文章对你有帮助，可以请我喝杯咖啡！"
              wechat_qr: ""
              alipay_qr: ""
          children:
            - $formkit: switch
              name: enable
              id: actionbar_enable
              label: 启用文章操作栏
              value: true
              help: "在文章版权声明上方显示点赞、分享、打赏操作卡片"
            - $formkit: switch
              name: readingFocus
              if: "$get(actionbar_enable).value"
              label: 沉浸阅读
              value: true
              help: "在操作栏显示「沉浸阅读」按钮：收起导航栏、两侧栏与目录，正文收成单栏居中；按 Esc 或点右上角按钮退出"
            - $formkit: switch
              name: like
              if: "$get(actionbar_enable).value"
              label: 启用点赞
              value: true
              help: "关闭后点赞图标仅展示、不可交互"
            - $formkit: switch
              name: share
              if: "$get(actionbar_enable).value"
              label: 启用分享
              value: true
              help: "显示分享按钮，点击生成分享海报"
            - $formkit: switch
              name: reward
              id: actionbar_reward
              if: "$get(actionbar_enable).value"
              label: 启用打赏
              value: true
              help: "显示打赏按钮，点击弹出微信/支付宝收款二维码"
            - $formkit: group
              name: rewardSetting
              label: 打赏设置
              if: "$get(actionbar_reward).value"
              value:
                title: "如果这篇文章对你有帮助，可以请我喝杯咖啡！"
                wechat_qr: ""
                alipay_qr: ""
              children:
                - $formkit: textarea
                  name: title
                  label: 打赏文案
                  value: "如果这篇文章对你有帮助，可以请我喝杯咖啡！"
                  help: "打赏弹窗顶部的自定义文案"
                - $formkit: attachment
                  name: wechat_qr
                  label: 微信收款码
                  accepts:
                    - "image/*"
                  aspectRatio: "1/1"
                  value: ""
                  help: "上传微信收款二维码图片（建议 1:1 正方形）"
                - $formkit: attachment
                  name: alipay_qr
                  label: 支付宝收款码
                  accepts:
                    - "image/*"
                  aspectRatio: "1/1"
                  value: ""
                  help: "上传支付宝收款二维码图片（建议 1:1 正方形）"
    - group: extendPages
      label: 扩展页面
      formSchema:
        - $formkit: group
          name: rssPage
          label: RSS 订阅页
          value:
            feedUrl: /rss.xml
            atomUrl: ""
            count: 5
          children:
            - $formkit: text
              name: feedUrl
              id: rssPage_feedUrl
              key: rssPage_feedUrl
              label: RSS 订阅地址
              value: /rss.xml
              help: "「RSS 链接」卡片里展示并供访客复制的地址；装了 Feed 插件时通常是 /rss.xml"
            - $formkit: text
              name: atomUrl
              id: rssPage_atomUrl
              key: rssPage_atomUrl
              label: Atom 订阅地址
              help: "可选；留空则只显示 RSS 一条"
            - $formkit: number
              name: count
              id: rssPage_count
              key: rssPage_count
              label: 展示文章数
              value: 5
              min: 1
              max: 20
              step: 1
              help: "「最新文章」列表展示的条数，默认 5 篇"
            - $formkit: array
              name: readers
              id: rssPage_readers
              key: rssPage_readers
              label: 推荐阅读器
              help: "「推荐阅读器」区块的条目；清空则整个区块不显示"
              value:
                - name: Folo
                  url: https://follow.is/
                  note: 设计简洁，中文友好
                - name: Inoreader
                  url: https://www.inoreader.com/
                  note: 功能强大，免费版够用
                - name: Feedly
                  url: https://feedly.com/
                  note: 界面现代，上手快
                - name: Readwise Reader
                  url: https://readwise.io/read
                  note: 适合做笔记的稍后阅读工具
              children:
                - $formkit: text
                  name: name
                  label: 名称
                  validation: required
                - $formkit: url
                  name: url
                  label: 链接
                  validation: required
                - $formkit: text
                  name: note
                  label: 一句话说明
        - $formkit: group
          name: friends
          label: 朋友圈页面
          help: "请先在「页面 → 自定义页面」中创建对应的页面，然后在此配置页面内容"
          children:
            - $formkit: number
              name: pageSize
              label: 每次加载数量
              value: 30
              min: 6
              max: 100
              step: 6
              help: "点击「加载更多」按钮时，每次加载的动态数量"
            - $formkit: number
              name: fetchLimit
              label: 拉取总条数
              value: 20
              min: 10
              max: 100
              step: 10
              help: "从链接管理插件拉取的动态总条数，最大100条。建议设置大于「每次加载数量」"
            - $formkit: switch
              name: enable_random_fish
              label: 显示随机钓鱼按钮
              value: true
              help: "在朋友圈页面标题右侧显示随机钓鱼按钮，点击随机跳转到一个朋友圈文章"
            - $formkit: textarea
              name: blacklist
              label: 屏蔽列表
              value: ""
              help: "要屏蔽的博主名称，每行一个。匹配到的动态将不会在朋友圈显示"
        - $formkit: group
          name: timeline
          label: 时间轴页面
          help: "请先在「页面 → 自定义页面」中创建对应的页面，然后在此配置页面内容"
          children:
            - $formkit: text
              name: subtitle
              label: 副标题
              value: "我的成长历程和重要里程碑"
              help: "显示在时间轴页面标题下方，留空则使用默认文案"
            - $formkit: number
              name: pageSize
              label: 每页数量
              value: 10
              min: 1
              max: 50
              step: 1
              help: "每页显示的时间轴条目数量，设为 0 或留空则不分页"
            - $formkit: group
              name: entries
              label: 时间轴条目
              children:
                - $formkit: array
                  name: items
                  label: 条目
                  help: "按时间顺序添加条目（建议新的在前）。开始/结束时间用日期选择器选择，结束时间留空表示「至今」"
                  children:
                    - $formkit: text
                      name: title
                      label: 标题
                      validation: required
                    - $formkit: select
                      name: type
                      label: 类型
                      value: education
                      options:
                        - label: 教育经历
                          value: education
                        - label: 工作经历
                          value: work
                        - label: 项目经历
                          value: project
                        - label: 成就荣誉
                          value: achievement
                    - $formkit: textarea
                      name: description
                      label: 描述
                    - $formkit: date
                      name: startDate
                      label: 开始时间
                      help: "选择开始日期，留空可只填结束时间"
                    - $formkit: date
                      name: endDate
                      label: 结束时间
                      help: "留空表示「至今」"
                    - $formkit: text
                      name: location
                      label: 地点
                    - $formkit: text
                      name: organization
                      label: 组织 / 机构
                    - $formkit: text
                      name: position
                      label: 职位 / 角色
                    - $formkit: textarea
                      name: skills
                      label: 技能标签
                      help: "每行一个"
                    - $formkit: textarea
                      name: achievements
                      label: 成就 / 亮点
                      help: "每行一个"
                    - $formkit: textarea
                      name: links
                      label: 相关链接
                      help: "每行一条，支持「名称|链接」格式（如 GitHub|https://github.com），也可直接填链接；统一在新窗口打开"
                    - $formkit: iconify
                      name: icon
                      label: 图标
                      format: svg
                      help: "卡片图标，留空则按类型自动匹配"
                    - $formkit: color
                      name: color
                      label: 主题色
                      value: ""
                      help: "节点与卡片图标的颜色，留空使用主题色"
                    - $formkit: switch
                      name: featured
                      label: 精选
                      value: false
                      help: "开启后在标题旁显示星标"
        - $formkit: group
          name: skills
          label: 技能页面
          help: "请先在「页面 → 自定义页面」中创建对应的页面，然后在此配置页面内容"
          children:
            - $formkit: text
              name: subtitle
              label: 副标题
              value: "我的技术技能和专业知识"
              help: "显示在技能页面标题下方，留空则使用默认文案"
            - $formkit: number
              name: pageSize
              label: 每页数量
              value: 10
              min: 1
              max: 50
              step: 1
              help: "每页显示的技能卡片数量，设为 0 或留空则不分页"
            - $formkit: group
              name: entries
              label: 技能列表
              children:
                - $formkit: array
                  name: items
                  label: 技能
                  help: "添加你的技能，每项生成一张卡片"
                  children:
                    - $formkit: text
                      name: name
                      label: 技能名称
                      validation: required
                    - $formkit: textarea
                      name: description
                      label: 描述
                    - $formkit: iconify
                      name: icon
                      label: 图标
                      format: svg
                      help: "技能图标，留空则显示名称首字母"
                    - $formkit: select
                      name: category
                      label: 分类
                      value: frontend
                      options:
                        - label: 前端开发
                          value: frontend
                        - label: 后端开发
                          value: backend
                        - label: 数据库
                          value: database
                        - label: 开发工具
                          value: tools
                        - label: 其他技能
                          value: other
                    - $formkit: select
                      name: level
                      label: 熟练度
                      value: intermediate
                      options:
                        - label: 入门
                          value: beginner
                        - label: 中级
                          value: intermediate
                        - label: 高级
                          value: advanced
                        - label: 专家
                          value: expert
                    - $formkit: number
                      name: years
                      label: 经验（年）
                      value: 0
                      min: 0
                      max: 50
                      step: 1
                    - $formkit: number
                      name: months
                      label: 经验（月）
                      value: 0
                      min: 0
                      max: 11
                      step: 1
                    - $formkit: color
                      name: color
                      label: 主题色
                      value: ""
                      help: "图标底色与进度条颜色，留空使用主题色"
        - $formkit: group
          name: sponsor
          label: 打赏支持页
          help: "请先在「页面 → 自定义页面」中创建对应页面（模板选「打赏支持」），然后在此配置页面内容"
          children:
            - $formkit: text
              name: subtitle
              label: 副标题
              value: "你的支持是我持续创作的动力"
              help: "显示在页面标题下方，留空则使用默认文案"
            - $formkit: textarea
              name: intro
              label: 引导文案
              value: "如果我的内容对你有帮助，欢迎通过以下方式打赏我，你的支持是我持续创作的动力！"
              help: "页面顶部的主文案，留空则使用默认文案"
            - $formkit: textarea
              name: note
              label: 打赏用途
              value: "你的打赏将用于服务器维护、内容创作和功能开发，帮助我持续提供优质内容。"
              help: "说明打赏会被用在哪里；留空则整块不显示"
            - $formkit: array
              name: methods
              label: 打赏方式
              help: "每项一张卡片：填了二维码就展示收款码，只填链接就显示「前往打赏」按钮（两者都填时优先二维码）。留空的项不会出现在前台"
              value:
                - name: 微信
                  description: 使用微信扫码打赏
                  qr: ""
                  url: ""
                - name: 支付宝
                  description: 使用支付宝扫码打赏
                  qr: ""
                  url: ""
              children:
                - $formkit: text
                  name: name
                  label: 名称
                  validation: required
                  help: "如：微信 / 支付宝 / 爱发电 / ko-fi"
                - $formkit: textarea
                  name: description
                  label: 描述
                  help: "卡片里的一句话说明，如「使用微信扫码打赏」"
                - $formkit: attachment
                  name: qr
                  label: 收款二维码
                  accepts:
                    - "image/*"
                  aspectRatio: "1/1"
                  value: ""
                  help: "上传收款二维码图片（建议 1:1 正方形）；留空则改用下面的链接按钮"
                - $formkit: text
                  name: url
                  label: 打赏链接
                  value: ""
                  help: "外链打赏页地址（如爱发电 / ko-fi）；填了就在卡片下方显示「前往打赏」按钮"
                - $formkit: iconify
                  name: icon
                  label: 图标
                  format: svg
                  help: "卡片图标；留空则按名称自动匹配（微信 / 支付宝用品牌图标，ko-fi 用咖啡杯，其余用爱心）"
                - $formkit: color
                  name: color
                  label: 主题色
                  value: ""
                  help: "图标与卡片的强调色，留空使用主题色"
            - $formkit: switch
              name: showSponsors
              label: 显示打赏列表
              value: true
              help: "关闭后前台不显示「打赏列表」整块"
            - $formkit: array
              name: sponsors
              label: 打赏列表
              help: "按你想展示的顺序添加；名称填「匿名」即可匿名展示"
              value: []
              children:
                - $formkit: text
                  name: name
                  label: 名称
                  validation: required
                - $formkit: text
                  name: amount
                  label: 金额
                  help: "自由填写，如 ¥50 / 一杯咖啡"
                - $formkit: date
                  name: date
                  label: 日期
                - $formkit: attachment
                  name: avatar
                  label: 头像
                  accepts:
                    - "image/*"
                  aspectRatio: "1/1"
                  value: ""
                  help: "留空则用名称首字代替"
            - $formkit: textarea
              name: thanks
              label: 结尾致谢
              value: "谢谢每一位支持过我的朋友，我会继续写下去。"
              help: "显示在打赏列表下方；留空则不显示"
    - group: footer
      label: 页脚
      formSchema:
        - $formkit: group
          name: runningTime
          label: 站点运行时间
          value:
            enable: false
            startDate: ""
            link: /about
          children:
            - $formkit: switch
              name: enable
              id: runningTime_enable
              key: runningTime_enable
              label: 启用
              value: false
              help: "在页脚显示「本站已运行 x 天 x 小时 x 分钟 x 秒」，秒数实时走动；未填上线时间时不显示"
            - $formkit: date
              name: startDate
              id: runningTime_startDate
              key: runningTime_startDate
              if: "$get(runningTime_enable).value"
              label: 站点上线时间
              help: "从这一刻开始计时；留空则该行不显示"
            - $formkit: text
              name: link
              id: runningTime_link
              key: runningTime_link
              if: "$get(runningTime_enable).value"
              label: 点击跳转地址
              value: /about
              help: "默认跳转到「关于」页面；留空则不可点击"
        - $formkit: group
          name: beian
          label: 备案信息
          value:
            icp_text: ""
            icp_link: "https://beian.miit.gov.cn/"
            gongan_text: ""
            gongan_link: "https://beian.mps.gov.cn/#/query/webSearch"
          children:
            - $formkit: text
              name: icp_text
              label: ICP备案号
            - $formkit: text
              name: icp_link
              label: ICP备案跳转链接
              value: https://beian.miit.gov.cn/
            - $formkit: text
              name: gongan_text
              label: 公安联网备案号
            - $formkit: text
              name: gongan_link
              label: 公安联网备案跳转链接
              value: https://beian.mps.gov.cn/#/query/webSearch
        - $formkit: group
          name: displayLinks
          label: 显示链接
          value:
            enable_privacy: false
            privacy_url: "/privacy"
            enable_rss: true
            enable_sitemap: true
          children:
            - $formkit: switch
              name: enable_privacy
              id: privacy_enable
              value: false
              label: 显示隐私政策
              help: "在页脚显示隐私政策链接"
            - $formkit: text
              name: privacy_url
              if: "$get(privacy_enable).value"
              label: 隐私政策链接
              value: /privacy
              help: "隐私政策页面地址"
            - $formkit: switch
              name: enable_rss
              value: true
              label: 显示 RSS
              help: "在页脚显示 RSS 链接（需安装 RSS 插件）"
            - $formkit: switch
              name: enable_sitemap
              value: true
              label: 显示 Sitemap
              help: "在页脚显示 Sitemap 链接（需安装 Sitemap 插件）"
            - $formkit: switch
              name: enable_theme_credit
              value: true
              label: 显示主题署名
              help: "开启后在页脚显示「Powered by Halo & Onlynn」标识，让访客了解您所使用的主题。"
        - $formkit: group
          name: customLinks
          label: 自定义链接
          children:
            - $formkit: array
              name: items
              label: 链接项
              help: "在页脚添加自定义超链接或HTML内容，填写HTML后优先渲染HTML，覆盖名称和链接"
              children:
                - $formkit: text
                  name: name
                  label: 名称
                  validation: "required"
                - $formkit: url
                  name: url
                  label: 链接
                - $formkit: textarea
                  name: html
                  label: 自定义HTML
                  help: "填写后优先渲染HTML，覆盖名称和链接"
        - $formkit: group
          name: friendLinks
          label: 页脚友情链接
          value:
            enable_friend_links: false
            is_home_only: false
            show_apply_btn: true
            max_items: 0
          children:
            - $formkit: switch
              name: enable_friend_links
              id: friend_links_enable
              value: false
              label: 显示友情链接
              help: "在页脚显示友情链接卡片墙，数据来自「链接管理」插件。"
            - $formkit: switch
              name: is_home_only
              if: "$get(friend_links_enable).value"
              key: friend_links_is_home_only
              preserve: true
              value: false
              label: 仅在首页显示
              help: "开启后卡片墙只出现在首页页脚"
            - $formkit: switch
              name: show_apply_btn
              if: "$get(friend_links_enable).value"
              key: friend_links_show_apply_btn
              preserve: true
              value: true
              label: 显示申请友链按钮
              help: "在标题右侧显示「申请友链」按钮，点击跳转到友情链接 /links 页面"
            - $formkit: number
              name: max_items
              if: "$get(friend_links_enable).value"
              key: friend_links_max_items
              preserve: true
              value: 0
              min: 0
              label: 最多显示条数
              help: "超出条数时在卡片下方显示下拉箭头，点击跳转到完整友链页；填 0 表示全部显示"
        - $formkit: group
          name: linkColumns
          label: 页脚链接栏
          value:
            enable: false
            title: ""
            menu: ""
            homeOnly: false
            columns: "4"
          children:
            - $formkit: switch
              name: enable
              id: linkColumns_enable
              label: 启用页脚链接栏
              value: false
              help: "在页脚显示多栏链接区（参考「导航 / 组织 / 协议 / 友链」那种分栏）。链接内容来自 Halo 菜单"
            - $formkit: text
              name: title
              if: "$get(linkColumns_enable).value"
              label: 区块标题
              value: ""
              help: "显示在链接栏上方的标题，与页脚「友情链接」同一套样式；留空则不显示标题行"
            - $formkit: menuSelect
              name: menu
              id: linkColumns_menu
              if: "$get(linkColumns_enable).value"
              label: 菜单
              value: ""
              help: "留空使用系统主菜单。建议为页脚单独建一个菜单（外观 → 菜单 → 新建），一级项当栏目标题、二级项当链接；一级项没有子项时会把自身作为该栏的唯一链接"
            - $formkit: switch
              name: homeOnly
              if: "$get(linkColumns_enable).value"
              label: 仅在首页显示
              value: false
              help: "开启后链接栏只出现在首页页脚"
            - $formkit: select
              name: columns
              if: "$get(linkColumns_enable).value"
              label: 每行列数
              value: "4"
              options:
                - label: 两列
                  value: "2"
                - label: 三列
                  value: "3"
                - label: 四列（推荐）
                  value: "4"
                - label: 五列
                  value: "5"
                - label: 六列
                  value: "6"
              help: "桌面端每行显示几栏；窄屏自动降为两列。栏数多于设定时自动换到下一行"
    - group: links
      label: 友情链接
      formSchema:
        - $formkit: group
          name: features
          label: 功能开关
          value:
            enable_comment: true
            enable_apply_btn: true
            enable_random_visit: true
            random_visit_groups: ""
          children:
            - $formkit: switch
              name: enable_comment
              value: true
              label: 显示评论
              help: "在友链页面底部显示评论区"

            - $formkit: switch
              name: enable_apply_btn
              value: true
              label: 显示申请友链按钮
              help: "在友链页面申请友链流程卡片下方显示「申请友链」按钮，点击弹出申请表单。需安装 Links 插件并开启访客提交友链功能"

            - $formkit: switch
              name: enable_random_visit
              id: random_visit_enable
              value: true
              label: 显示随机访问按钮
              help: "在友链页面标题右侧显示随机访问按钮，点击随机跳转到一个友链"
            - $formkit: textarea
              name: random_visit_groups
              if: "$get(random_visit_enable).value"
              label: 随机访问分组
              value: ""
              help: "限制随机访问的友链分组，每行输入一个分组名称。留空则表示所有分组都参与随机访问"
        - $formkit: group
          name: ownerInfo
          label: 我的博客信息
          value:
            owner_avatar: "https://example.com/avatar.jpg"
            owner_name: "博客名称"
            owner_description: "这是一个基于 Halo 搭建的个人博客"
            owner_url: "https://example.com"
            owner_rss: "https://example.com/rss.xml"
          children:
            - $formkit: text
              name: owner_avatar
              label: 博客头像
              value: https://example.com/avatar.jpg
              help: "博客头像图片地址"
            - $formkit: text
              name: owner_name
              label: 博客名称
              value: 博客名称
              help: "博客名称"
            - $formkit: textarea
              name: owner_description
              label: 博客描述
              value: 这是一个基于 Halo 搭建的个人博客
              help: "博客简短描述"
            - $formkit: text
              name: owner_url
              label: 博客网址
              value: https://example.com
              help: "博客首页地址"
            - $formkit: text
              name: owner_rss
              label: RSS 地址
              value: https://example.com/rss.xml
              help: "博客 RSS 订阅地址"
        - $formkit: group
          name: applyFlow
          label: 申请友链流程
          children:
            - $formkit: array
              name: applySteps
              label: 申请步骤
              help: "自定义申请友链的步骤，最多5步，支持HTML。填写几个就显示几个步骤"
              max: 5
              value:
                - title: "添加本站友链"
                  desc: "请先在您的网站友链页面<strong>添加本站信息</strong>，可直接复制左侧各字段"
                - title: "评论留言"
                  desc: "在本页下方评论区留言提交申请"
                - title: "等待审核"
                  desc: "确认信息无误后会尽快添加您的友链"
              children:
                - $formkit: text
                  name: title
                  label: 步骤标题
                  validation: required
                - $formkit: textarea
                  name: desc
                  label: 步骤说明 (支持HTML)
                  validation: required
        - $formkit: group
          name: accordionPanel
          label: 折叠面板
          children:
            - $formkit: array
              name: accordions
              label: 面板项
              help: "自定义折叠面板，支持 HTML。"
              value:
                - title: "友链须知"
                  content: |
                    <ul>
                    <li>请先在您的网站添加本站信息</li>
                    <li>网站能够正常访问，使用HTTPS</li>
                    <li>网站符合中国大陆法律法规</li>
                    </ul>
                - title: "免责申明"
                  content: |
                    <ul>
                    <li>本博客严格遵守中华人民共和国法律法规，友情链接及其他外链，仅为了方便学习与交流而收集整理</li>
                    <li>本人能力有限，无法逐一甄别每篇文章的全部内容，也无法实时跟进并确认原作者后续是否对链接内容进行篡改，包括但不限于发布违法信息、植入恶意代码、诱导点击或破坏用户计算机等行为</li>
                    </ul>
              children:
                - $formkit: text
                  name: title
                  label: 面板标题
                  validation: required
                - $formkit: iconify
                  name: icon
                  label: 图标
                  format: svg
                - $formkit: textarea
                  name: content
                  label: 正文 (支持HTML)
                  validation: required
    - group: external_link
      label: 外链跳转
      formSchema:
        - $formkit: switch
          name: enable_redirect
          id: redirect_enable
          label: 启用外链跳转提示
          value: true
          help: "开启后点击外部链接时显示跳转提示页面"
        - $formkit: number
          name: redirect_delay
          if: "$get(redirect_enable).value"
          label: 自动跳转时间（秒）
          min: 0
          max: 30
          step: 1
          value: 5
          help: "设置为 0 则禁用自动跳转"
        - $formkit: textarea
          name: redirect_prompt
          if: "$get(redirect_enable).value"
          label: 自定义提示文本
          value: "您即将访问外部链接，请注意保护个人隐私和信息安全"
          help: "自定义外链跳转模态框中显示的提示语句"
        - $formkit: attachment
          name: avatar
          if: "$get(redirect_enable).value"
          label: 提示页头像
          help: "留空则使用默认图标"
        - $formkit: textarea
          name: whitelist
          if: "$get(redirect_enable).value"
          label: 域名白名单
          value: ""
          help: "白名单内的域名不显示跳转提示，直接跳转。每行一个域名，支持通配符如 *.example.com"
        - $formkit: switch
          name: open_new_window
          if: "$get(redirect_enable).value"
          label: 新窗口打开
          value: true
          help: "开启后将在新标签页打开外部链接"
    - group: enhance
      label: 增强功能
      formSchema:
        - $formkit: group
          name: tabTitle
          label: 离屏文案
          value:
            enable: true
            hidden: ""
            visible: ""
            hold: 2
          children:
            - $formkit: switch
              name: enable
              id: tabTitle_enable
              label: 启用
              value: true
              help: "浏览器切到其它标签页 / 后台时，把标签页标题换成下面的文案"
            - $formkit: text
              name: hidden
              id: tabTitle_hidden
              if: "$get(tabTitle_enable).value"
              label: 离屏文案（离开）
              value: ""
              help: "离开当前标签时显示的标题；留空则保持原标题不变"
            - $formkit: text
              name: visible
              id: tabTitle_visible
              if: "$get(tabTitle_enable).value"
              label: 离屏文案（回来）
              value: ""
              help: "回到当前标签时短暂显示、随后自动恢复原标题的文案；留空则直接恢复"
            - $formkit: number
              name: hold
              if: "$get(tabTitle_enable).value"
              label: 回来文案停留秒数
              value: 2
              min: 1
              max: 30
              step: 1
              help: "「回来」文案显示多久后恢复原标题"
        - $formkit: group
          name: cursor
          label: 鼠标风格
          value:
            style: none
            img_default: ""
            img_pointer: ""
            img_text: ""
            img_zoom: ""
            hotspot_x: 0
            hotspot_y: 0
          children:
            - $formkit: select
              name: style
              id: cursor_style
              label: 光标样式
              value: none
              options:
                - value: none
                  label: 关闭（系统光标）
                - value: OwO
                  label: OwO
                - value: UwU
                  label: UwU
                - value: breeze
                  label: 清风（深色）
                - value: mellow
                  label: 卡通圆润
                - value: water_01
                  label: 彩虹水滴（一）
                - value: water_02
                  label: 彩虹水滴（二）
                - value: horse
                  label: 彩虹小马
                - value: debris
                  label: 彩色碎片
                - value: overwatch
                  label: 守望先锋
                - value: rainbow_rain
                  label: 彩虹云雨
                - value: marry
                  label: 小樱茉莉
                - value: black_cat
                  label: 黑色小猫
                - value: music_cat_01
                  label: 音乐小猫（一）
                - value: music_cat_02
                  label: 音乐小猫（二）
                - value: custom
                  label: 自定义上传
              help: "每套都是指针 / 手型 / 文本 / 缩放四态。前三套以外的素材四态用同一对文件，其余少数几套四态各自不同。触摸设备上无效"
            - $formkit: attachment
              name: img_default
              if: "$get(cursor_style).value === 'custom'"
              label: 默认指针
              accepts:
                - ".cur"
                - ".ani"
                - "image/*"
              value: ""
              help: "建议 .cur（含热点）；用 .png / .svg 时请填下面的热点偏移。留空则用系统默认指针"
            - $formkit: attachment
              name: img_pointer
              if: "$get(cursor_style).value === 'custom'"
              label: 手型（链接 / 按钮）
              accepts:
                - ".cur"
                - ".ani"
                - "image/*"
              value: ""
              help: "鼠标移到链接、按钮上时显示；留空则用系统默认"
            - $formkit: attachment
              name: img_text
              if: "$get(cursor_style).value === 'custom'"
              label: 文本（正文可选中区域）
              accepts:
                - ".cur"
                - ".ani"
                - "image/*"
              value: ""
              help: "鼠标移到正文文字上时显示；留空则用系统默认"
            - $formkit: attachment
              name: img_zoom
              if: "$get(cursor_style).value === 'custom'"
              label: 缩放（图片）
              accepts:
                - ".cur"
                - ".ani"
                - "image/*"
              value: ""
              help: "鼠标移到正文图片上时显示；留空则用系统默认"
            - $formkit: number
              name: hotspot_x
              if: "$get(cursor_style).value === 'custom'"
              label: 热点偏移 X
              value: 0
              min: 0
              max: 64
              step: 1
              help: "光标文件的「点击点」水平像素位置；.cur / .ani 自带热点，填了也不生效（仅对 png / svg 有意义）"
            - $formkit: number
              name: hotspot_y
              if: "$get(cursor_style).value === 'custom'"
              label: 热点偏移 Y
              value: 0
              min: 0
              max: 64
              step: 1
              help: "光标文件的「点击点」垂直像素位置，同上"
        - $formkit: group
          name: cursorFx
          label: 鼠标特效
          value:
            move: none
            emoji: "🌸,✨,💗,⭐,🎈"
            click: none
            words: "富强,民主,文明,和谐,自由,平等,公正,法治,爱国,敬业,诚信,友善"
            opacity: 1
            mobile: false
          children:
            - $formkit: select
              name: move
              id: cursorFx_move
              label: 鼠标移动特效
              value: none
              options:
                - value: none
                  label: 关闭
                - value: bubbleCursor
                  label: 气泡跟随
                - value: emojiCursor
                  label: 表情包跟随
                - value: springyEmojiCursor
                  label: 弹性表情包跟随
                - value: fairyDustCursor
                  label: 仙女棒效果
                - value: snowflakeCursor
                  label: 雪花跟随
                - value: followingDotCursor
                  label: 圆点跟随
                - value: ghostCursor
                  label: 移动残影（疏）
                - value: trailingCursor
                  label: 移动残影（密）
              help: "跟随鼠标移动的特效。移动端与「减少动效」偏好下默认不启用"
            - $formkit: text
              name: emoji
              id: cursorFx_emoji
              if: "$get(cursorFx_move).value === 'emojiCursor' or $get(cursorFx_move).value === 'springyEmojiCursor'"
              label: 跟随表情列表
              value: "🌸,✨,💗,⭐,🎈"
              help: "用英文逗号分隔，随机取用"
            - $formkit: select
              name: click
              id: cursorFx_click
              label: 鼠标点击特效
              value: none
              options:
                - value: none
                  label: 关闭
                - value: firework
                  label: 烟花特效
                - value: granule
                  label: 粒子爆炸
                - value: prosperous
                  label: 富强民主
                - value: heart
                  label: 爱心特效
              help: "在点击位置迸发的特效"
            - $formkit: text
              name: words
              id: cursorFx_words
              if: "$get(cursorFx_click).value === 'prosperous'"
              label: 上浮文字列表
              value: "富强,民主,文明,和谐,自由,平等,公正,法治,爱国,敬业,诚信,友善"
              help: "用英文逗号分隔，随机取用"
            - $formkit: number
              name: opacity
              id: cursorFx_opacity
              label: 特效不透明度
              value: 1
              min: 0.1
              max: 1
              step: 0.1
              help: "想把特效调淡一些就调小；1 为完全不透明"
            - $formkit: switch
              name: mobile
              id: cursorFx_mobile
              label: 移动端特效显示
              value: false
              help: "触摸设备上默认不显示鼠标特效，以免手指滑动时满屏粒子"
        - $formkit: group
          name: grayMode
          label: 灰色模式
          value:
            enable: false
            ranges: ""
          children:
            - $formkit: switch
              name: enable
              id: grayMode_enable
              label: 启用
              value: false
              help: "全站以灰度显示，常用于哀悼日"
            - $formkit: textarea
              name: ranges
              id: grayMode_ranges
              if: "$get(grayMode_enable).value"
              label: 生效日期段
              value: ""
              rows: 3
              help: "每行一段，格式 MM/DD~MM/DD，例如 12/13~12/13。留空则长期生效；可跨年，如 12/30~01/02"
    - group: performance
      label: 速度优化
      formSchema:
        - $formkit: group
          name: imageProcessing
          label: 图片处理
          value:
            provider: none
            card_cover_width: 400
            article_image_width: 1200
            custom_format: ""
            banner_width: 1920
            avatar_size: 512
          children:
            - $formkit: select
              name: provider
              id: image_processing_provider
              label: 图片处理服务
              value: none
              help: "选择你的图片存储/CDN 服务，以启用实时图片压缩缩放。此功能会对文章封面图和文章内图片进行分辨率压缩处理，以便达到速度优化效果。\n如果图片存储在非halo内部存储，需在对应服务端开启图片处理功能。\n建议值仅供参考，以实际显示效果自主调试为准。\n此功能测试中，无法保证所有功能正常，如有问题请反馈。"
              options:
                - label: 不处理（使用原图）
                  value: none
                - label: Halo 内置附件存储
                  value: halo
                - label: 阿里云 ESA / DCDN
                  value: aliyun_esa
                - label: 阿里云 OSS
                  value: aliyun_oss
                - label: 腾讯云 EdgeOne
                  value: tencent_eo
                - label: 腾讯云 COS（数据万象）
                  value: tencent_cos
                - label: 七牛云
                  value: qiniu
                - label: 又拍云
                  value: upyun
                - label: 自定义
                  value: custom
            - $formkit: text
              name: custom_format
              if: "$get(image_processing_provider).value === 'custom'"
              label: 自定义参数格式
              value: ""
              help: "使用 {width} 作为宽度占位符。例如：?image_process=resize,w_{width}"
            - $formkit: number
              name: card_cover_width
              if: "$get(image_processing_provider).value !== 'none'"
              label: 首页卡片封面宽度 (px)
              value: 400
              min: 0
              max: 2000
              step: 50
              help: "建议 300-800，设为 0 则使用原图不压缩。原图宽度不足时仅支持缩小"
            - $formkit: number
              name: article_image_width
              if: "$get(image_processing_provider).value !== 'none'"
              label: 文章页图片宽度 (px)
              value: 1200
              min: 0
              max: 2000
              step: 50
              help: "建议 800-2000，设为 0 则使用原图不压缩。当使用内部存储时仅处理文章封面图，正文图片宽度由系统自动优化；使用 CDN 时封面图与正文图片均生效。原图宽度不足时仅支持缩小"
            - $formkit: number
              name: banner_width
              if: "$get(image_processing_provider).value !== 'none'"
              label: Banner 图片宽度 (px)
              value: 1920
              min: 0
              max: 3840
              step: 160
              help: "Banner 图片的缩放宽度（px）。设为 0 则使用原图不压缩。建议值：1920\n注：Banner 会按屏幕自适应加载——固定 400/800/1200/1600 档位；使用外部 CDN 时，本项宽度还会作为顶档参与选择（超过 1600 才生效）。本项宽度同时用作兜底地址（不支持 srcset 的浏览器）与首页预加载地址；外部 CDN 下设为 0 表示不分档，直接加载原图"
            - $formkit: number
              name: avatar_size
              if: "$get(image_processing_provider).value !== 'none'"
              label: 头像尺寸 (px)
              value: 512
              min: 0
              max: 512
              step: 32
              help: "头像图片的缩放宽度（px）。设为 0 则使用原图不压缩。建议值：512"
    - group: auth
      label: 认证
      formSchema:
        - $formkit: select
          name: loginPage
          id: login_page
          label: 登录界面
          value: halo
          options:
            - label: 主题认证界面
              value: theme
            - label: Halo 默认
              value: halo
