apiVersion: v1alpha1
kind: AnnotationSetting
metadata:
  name: onlynn-menuitem-icon
spec:
  targetRef:
    group: ""
    kind: MenuItem
  formSchema:
    - $formkit: iconify
      name: icon
      label: 图标
      format: svg
      value-only: true
      help: "菜单项左侧显示的图标，留空则不显示。图标颜色与尺寸自动跟随菜单文字，无需手动设置。"
