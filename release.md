## Onlynn v1.1.0

这一版以「页脚展示」为主线，同时修掉了一批外观配置相关的顽疾 —— 其中最要紧的一条是
**访客可调样式的总开关此前实际不生效**，后台关掉后访客仍能改样式，升级后恢复正常。
另外把 Hero 全屏布局从「只对首页生效」改成全局模式，并补齐了波浪、虚化、下箭头等细节。

### 新增

- **页脚链接栏** —— 页脚的多栏链接区（导航 / 组织 / 协议 / 友链 这种），栏目标题与链接
  都可在后台配置。数据直接用 **Halo 菜单**：一级菜单项作为栏目标题、二级项作为该栏链接，
  因此排序、嵌套、图标都能用 Halo 原生的菜单编辑器维护。整组按内容宽度居中，
  只配一两栏也不会贴着左边。设置项：**页脚 → 页脚链接栏**
- **副标题「打字机回退效果」开关** —— 开启后打字完成会逐字回退再重新打字，形成循环；
  关闭则打完即停。默认开启。设置项：**样式设置 → 标题与副标题**
- **Hero 内页背景虚化** —— 全屏 + Hero 下首页保持清晰（滚动时逐步虚化），其余页面的
  壁纸按此值常驻虚化，让浮在壁纸上的内容更清楚。默认 8px，填 0 可关闭。
  设置项：**样式设置 → 显示设置面板 → 壁纸默认值**
- **友链页「一键复制全部信息」** —— 左侧自身信息卡底部一个按钮，一次复制五行：
  站点名称 / 站点描述 / 站点链接 / 头像链接 / RSS 地址，方便友链申请时直接粘贴

### 修复

- **访客可调样式的总开关不生效（影响最大）** —— 访客开关的配置路径在改版时迁到了新位置，
  但前台仍在读旧路径（站点配置里两处同时存在、旧值恒为开启），导致后台关掉总开关后，
  前台的设置按钮与面板照样出现、访客仍能改样式。已统一到新路径；顺带补齐了入口与面板的
  开关白名单（原先漏了樱花 / 弹幕 / 侧栏位置三项，会出现「只开樱花却看不到入口」）
- **Hero 模式现在对所有页面生效** —— 原先只对首页生效，文章页与其他页面仍是普通全屏
  横幅。现在只要「全屏 + Hero」两处设置命中，所有页面都是固定整屏壁纸 + 滚动虚化 + 标题渐隐
- **Hero 下壁纸被下推、上方留白** —— 壁纸带了一处纵向取景偏移而首页之外没有兜底归零，
  导致壁纸整体下推约 270px、上方露出一条页面底色
- **Hero 下自动关闭波浪** —— 波浪锚定在壁纸底边，而 Hero 的壁纸是固定整屏背景、没有
  可贴的边；现在 Hero 下整块隐藏波浪，面板里的「波浪」开关也同步收起（只在「横幅」与
  「全屏 + 经典」下出现）
- **首页向下箭头在浅色壁纸上看不清** —— 保持原有的大箭头造型不加底衬，白色加深到 95%，
  投影改为两层，浅色壁纸上也能看清

### 变更

- **归属表述调整**：基础样式取自 [Fuwari](https://github.com/saicaca/fuwari)，移植到 Halo
  后按 [Firefly](https://github.com/CuteLeaf/Firefly) 的美学补齐外观与交互定制能力。
  各上游项目的版权声明完整保留在 [LICENSE](./LICENSE)
- 主题自身的占位文本与内部标识统一为 `Onlynn` / `onlynn-`，其中访客端的本地记录键
  （欢迎弹窗关闭标记、公告关闭标记、点赞、瞬间点赞）改名后，**访客端旧记录会失效一次**
  （欢迎弹窗会再弹一次、点赞状态归零），属预期

### 安装与升级

1. 下载下方的 `onlynn-1.1.0.zip`
2. 全新安装：Halo 后台 →「外观」→「主题」→ 安装 → 上传 zip
3. 从旧版升级：主题列表 → 目标主题 → 升级 → 上传 zip。主题配置与页面数据不受影响

> 需要 Halo >= 2.25.0。推荐一并安装 Feed（RSS）、Sitemap、Shiki（代码高亮）插件；
> 评论弹幕需要评论插件（实现 `CommentWidget` 扩展点的那个）。

更细的逐条变更见 [CHANGELOG.md](./CHANGELOG.md)。
