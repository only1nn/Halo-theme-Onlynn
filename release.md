## Onlynn v1.2.0

「增强功能」独立成组（第 11 个）：**离屏文案 / 鼠标风格 / 鼠标特效 / 灰色模式**；文章新增
「热度 ℃」；搜索与超链接卡片的配色也对齐到主题。

这一版还修掉了三个**会让整页不可用**的问题（留言板打不开、留言板正文消失、错误页自身报错），
并做了三处加载优化（图片按槽位取尺寸、副标题首屏渲染、鼠标特效应需启动）。

> ⚠️ **如果你手上是更早的 1.2.0 包，请换用这一版** —— 那批包里带着「打开留言板整页打不开」的
> 致命问题。1.2.0 尚未正式发布，本次修复全部并入同一版本号。

### 新增

- **离屏文案** —— 切到其它标签页 / 后台时把标签标题换成「离开」文案，回来时短暂显示「回来」
  文案再自动恢复，停留秒数可配；换页时原标题会跟着更新。设置项：**增强功能 → 离屏文案**
- **鼠标风格** —— **14 套内置光标**（OwO / UwU / 清风 / 卡通圆润 / 彩虹水滴 ×2 / 彩虹小马 /
  彩色碎片 / 守望先锋 / 彩虹云雨 / 小樱茉莉 / 黑色小猫 / 音乐小猫 ×2），每套都是指针 / 手型 /
  文本 / 缩放四态；另有「自定义上传」可换成你自己的光标文件。设置项：**增强功能 → 鼠标风格**
- **鼠标特效** —— 移动 8 种（气泡跟随 / 表情包跟随 / 弹性表情包跟随 / 仙女棒效果 / 雪花跟随 /
  圆点跟随 / 移动残影（疏）/ 移动残影（密））+ 点击 4 种（烟花特效 / 粒子爆炸 / 富强民主 /
  爱心特效），词表可改，另有不透明度与移动端开关。设置项：**增强功能 → 鼠标特效**
- **灰色模式** —— 哀悼日等场景按日期段全站去色，支持多段与跨年段（如 `12/30~01/02`）。
  设置项：**增强功能 → 灰色模式**
- **文章热度 ℃** —— 按 `24 + 浏览量×0.1 + 点赞×2 + 评论×3` 算一个热度值，分三档配色显示在
  文章信息栏（列表卡片与文章页都会显示）。设置项：**文章 → 文章信息 → 显示文章热度**（默认关）
- **插件的 CSS 变量对齐** —— 搜索组件与编辑器超链接卡片的官方 CSS 变量映射到主题配色与圆角，
  插件页面不再跳出色系。纯 CSS，无脚本、无新增依赖

### 加载优化

- **图片按槽位取尺寸（这一项省得最多）** —— Halo 2.22 起会给没写 `sizes` 的图片按「整栏宽度」
  猜一个默认值，于是 154px 宽的卡片封面也去取 800px 的图、28px 的导航栏 logo 也取了 800px
  （256KB）。现在主题给卡片封面、站点 logo、菜单图标、页脚友链 logo、音乐播放器封面都写了
  准确的 `sizes`：实测同一张封面从 `?width=800` 变成 `?width=400`，单张体积降到约三分之一
- **副标题首屏即渲染** —— 不再等 banner 脚本（defer 加载 + 开局再停 500ms）才逐字打出来；
  现在首屏就能看到完整副标题，打字 / 回退动效随后照常循环
- **鼠标特效「按需启动」** —— 原先打开任意页面都会立刻建一整屏特效画布并预烘一批离屏精灵，
  即使你根本不会动鼠标；现在都推迟到第一次移动（或点击）。实测首页 DOMContentLoaded
  从 537ms 降到 490ms；只开「点击特效」时连一次文本测量都不做
- **两侧边栏不再做入场淡入** —— 边栏内容是静态的、位置在页面两端，这条淡入观感收益很小，
  却是「侧栏偶尔晚一秒才出现」的唯一来源；现在它们跟着首帧一起出现

### 鼠标特效的性能与无障碍

原实现是每个特效一个脚本、九个永不停止的逐帧循环、指针事件不节流、只有粒子爆炸有上限、
爱心每次点击都往 DOM 里塞节点。这里合成单画布 + 对象池 + 按需逐帧，并补齐：

- **粒子有硬上限 + 对象池**：移动 ≤150、单次点击 ≤60、总数 ≤300，粒子回收复用
- **没有粒子就停掉逐帧循环**，下一个指针事件再启动 —— 不常驻后台（跟随类特效停下也不再排帧）
- **标签页隐藏时停帧**，切回来接着跑（粒子保留）
- **指针事件按距离 + 时间双阈值节流**，并合并到一帧里处理
- **DPR 感知**，窗口尺寸变化重建画布
- **尊重系统「减少动效」**：命中时完全不启动
- **触摸设备默认不启用**，可用「移动端特效显示」覆盖
- **关掉时不下载脚本**：两个特效都选「关闭」时一个请求都不发

### 修复

- **留言板整页打不开（影响最大）** —— 打开留言板会一直转圈、服务端抛
  `TemplateProcessingException: Could not parse as expression` 后中断响应，浏览器看到的是页面
  永远加载不完。根因：Thymeleaf 在 HTML 模式下默认会把文本里的内联标记当表达式处理，而音乐
  播放器的 LRC 时间戳正则里正好出现「方括号紧挨左圆括号」这三个字符，Thymeleaf 一路找配对的
  括号，把后面大段脚本拿去当表达式解析就崩了。改用十六进制转义写那两个方括号，语义完全一致
  （加 `th:inline="none"` 实测挡不住）
- **留言板「样式没了、正文不见了」** —— 上一条修完后页面能打开了，但只看得见开头一小段，
  后面的卡片、侧栏、页脚全不见。根因在构建期的注释剥离脚本：注释里出现的成对标签字样被当成了
  真正的样式块起点，它「保护」了注释后半截直到下一个闭合标签、同时删掉注释开头，留下一个裸露的
  开标签，浏览器把后面三十万字符的正文全当文本吞掉。已改成从左到右单遍扫描
- **错误页自身报错** —— 访问不存在的路径时连错误页都渲染不出来，根因与上面同类
- **侧栏偶尔「过一秒才显示出来」** —— 入场动画是「透明 + 动画补间到可见」的写法，摘类的唯一
  时机是 `animationend`；动画被浏览器挂起（后台标签页 / 主线程被占）或被重建后重放时那个事件
  永远不来，元素就一直隐形。除了不再给边栏做淡入，其余入场元素也加了兜底清扫（比对动画进度，
  卡住就摘类回到可见），保证内容不可能一直不可见
- **跟随类特效（圆点 / 两种残影 / 弹性表情）在真实浏览器里看不见** —— 它们的「还有活干吗」判定
  原先按墙钟算，而后台标签页会把定时器节流到 1 秒一帧，于是「刚画完一帧就被判空闲、紧接着被收尾
  的清屏擦掉」。改成按帧计数，并且停下时保留最后一帧
- **「圆点跟随」停不下来** —— 淡入与淡出写在同一帧的两条不互斥分支里，净效果是每帧 +0.01
  不透明度，逐帧循环永远不退出
- **灰色模式不能用 `filter` 挂在 `html` 上** —— `filter` 会让该元素成为所有 fixed 后代的包含块，
  挂上去会让导航栏、浮动按钮、移动端底栏改成相对 `html` 盒子定位、跟着页面滚走。改用一层
  `backdrop-filter: grayscale(1)` 覆盖层
- **粒子迸发的两个调色板项明度写成 100%（等于纯白）** —— 叠色后糊成一团白斑；顺带让迸发粒子
  边飞边收小、末段淡出
- **灰色模式的坏配置** —— 日期段填了内容却一行都解析不出来时按「不生效」处理，否则一个笔误
  就会让全站长期变灰

### 变更

- 后台设置分组 10 → 11（新增「增强功能」）
- 主题首次打包第三方素材：14 套光标共 37 个 `.cur`（约 205KB）。这批素材来自 Dream2.0 Plus 的
  素材包（该项目未对其来源署名），出处与逐套文件对应表见 `public/assets/cursors/SOURCES.md`
- 「显示设置」面板的「特效」页签由 2 个开关增加到 4 个（新增自定义光标、鼠标特效），
  仍随「访客可调样式」总开关一起收回
- 「文章 → 文章信息」多一项「显示文章热度」（默认关）
- 一言的「获取失败文案」默认句子改为「愿做檐下一株草，不争不抢，自在生长。」（该字段在后台
  保存过的站点不受影响，想换新句子在「侧边栏 → 小组件设置 → 一言」里手工改）

### 安装与升级

1. 下载下方的 `onlynn-1.2.0.zip`
2. 全新安装：Halo 后台 →「外观」→「主题」→ 安装 → 上传 zip
3. 从旧版升级：主题列表 → 目标主题 → 升级 → 上传 zip。主题配置与页面数据不受影响

> 需要 Halo >= 2.25.0。推荐一并安装 Feed（RSS）、Sitemap、Shiki（代码高亮）插件；
> 评论弹幕需要评论插件（实现 `CommentWidget` 扩展点的那个）。
>
> **图片建议用 JPEG**（需要透明再 PNG）：Halo 的缩略图管线只支持 jpg/jpeg/png/bmp/wbmp，
> WebP 不会被缩放，会绕过上面那条「按槽位取尺寸」的优化。

更细的逐条变更见 [CHANGELOG.md](./CHANGELOG.md)。
